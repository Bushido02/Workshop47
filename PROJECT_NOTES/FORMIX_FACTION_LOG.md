# Formix Faction — журнал разработки

Этот файл — рабочий журнал для продолжения работы над фракцией Formix в моде
Reign of Nether (Forge 1.20.1). Веду его, чтобы в следующей сессии не
восстанавливать контекст заново, а сразу продолжать с точки остановки.

Обновляй этот файл при каждой сессии: что сделано, что решено, что осталось.

---

## 0. АКТУАЛЬНОЕ СОСТОЯНИЕ (читать в первую очередь)

**Этот раздел — источник правды.** Он ПОЛНОСТЬЮ ПЕРЕЗАПИСЫВАЕТСЯ в конце
каждой сессии, где что-то менялось (не дописывается построчно, не
накапливается хронологически). Если в следующей сессии здесь снова
появляется вторая по счёту запись "НОВОЕ (дата): ..." — это сигнал
остановиться и переписать раздел целиком, а не дописать ещё одну запись
поверх. Раздел был сжат 26.07.2026 (ночь) — до этого разросся до ~370
строк за счёт объёма работы над GeckoLib-терминалом и orthoview-багом в
одной сессии; все детали сохранены без потерь в архиве ниже (разделы
1.13–1.16). Разделы ниже (## 1 и далее) — архив истории по датам: причины
багов, разборы, уроки. Если архив противоречит этому разделу — прав ЭТОТ
раздел, архив мог устареть.

**Последнее обновление:** 26.07.2026, ночь.

### 🔴 Активный незавершённый баг — ждёт ПРАВИЛЬНОГО теста от пользователя

**"FormixHive не даёт population supply, иногда 0/0"** — причина не
найдена статическим анализом (раздел 12.2 архива). Два throttled
диагностических лога активны:
- `building/BuildingClientEvents.java:101` — `[FORMIX-DEBUG-CLIENT-SUPPLY]`
  в `getTotalPopulationSupply()`.
- `unit/UnitClientEvents.java:247` — `[FORMIX-DEBUG-CLIENT]` в
  `getCurrentPopulation()` (второй, отдельный баг: занятое население 0
  vs 3 при разных выбранных юнитах).

Первый присланный `latest.log` не содержал ни одной строки с этими
префиксами — причина в неправильной последовательности теста, не в
самой диагностике (см. раздел 1.16 архива (часть B) для точной процедуры теста,
которую нужно передать пользователю дословно перед следующей попыткой).

**Оба лога ОБЯЗАТЕЛЬНО убрать** после получения диагностики и исправления
бага — не должны остаться в финальном коде.

### 🟢 Недавно исправлено, ждёт подтверждения в игре

- **Полёт/noclip в Survival при использовании RTS-режима** — ТРИ отдельные
  причины, все исправлены за две последовательные попытки (первая
  оказалась неполной, см. раздел 1.17 архива для полного разбора):
  (1) не сбрасывался полёт при выходе из orthoview в течение сессии,
  (2) незакрытое статическое поле `OrthoviewClientEvents.enabled` не
  сбрасывалось на новый вход в мир, (3) — **настоящая причина** отчёта
  пользователя — `PlayerServerEvents.onPlayerJoin()` безусловно добавлял
  КАЖДОГО заходящего игрока в `orthoviewPlayers` при первом входе, из-за
  чего сервер сам считал любого игрока "в orthoview" с первого тика.
  Файлы: `mixin/PlayerMixin.java`, `player/PlayerServerEvents.java`,
  `orthoview/OrthoviewClientEvents.java`. НЕ проверено вживую после
  третьего фикса.
- **GeckoLib-терминал (control station)** — блок + модель + переход в
  RTS-режим по клику на нижнюю панель + item в руке с полноценной 3D
  GeckoLib-моделью + отдельная вкладка творческого режима "Formix".
  Полная история — разделы 1.13–1.17 архива. Ключевые факты:
  - Два раздельных блока (`FormixControlStationBlock` = активный,
    `FormixControlStationInactiveBlock` = неактивный), НЕ один блок с
    состоянием — решение пользователя.
  - GeckoLib подключается через Curse Maven
    (`curse.maven:geckolib-388172:5675221`), НЕ напрямую с Cloudsmith —
    прямые координаты `software.bernie.geckolib:...` не резолвились у
    пользователя.
  - Item в руке/инвентаре — полноценная 3D GeckoLib-модель через
    `GeoItem`/`IClientItemExtensions` (не авто-`BlockItem`), решение
    пользователя ("нетипично для проекта, но полноценная модель").
  - **Своя вкладка `CreativeModeTabRegistrar.FORMIX_TAB`** (26.07.2026,
    ночь) — все Formix-предметы/блоки собраны в одном месте, не
    разбросаны по ваниль-вкладкам. Терминал убран из
    `CreativeModeTabs.FUNCTIONAL_BLOCKS` (там дублировался раньше).
    Добавить новый Formix-предмет в будущем = одна строка в списке
    `FORMIX_TAB_ITEMS` внутри `CreativeModeTabRegistrar.java`, ничего
    больше трогать не нужно. См. раздел 4.5 `QUICK_REFERENCE.md` для
    общей процедуры добавления новых GeckoLib-блоков.
  - `blockstates/`+`models/block/`+`models/item/` JSON обязательны даже
    при `RenderShape.ENTITYBLOCK_ANIMATED` — без них блок/предмет
    считаются "missing model" в реестре Minecraft ещё до того, как
    GeckoLib получает шанс отрендерить что-то.
  - **НЕ проверено вживую:** компиляция Java против GeckoLib API,
    визуальная корректность модели в мире и в руке (координатная
    конвертация `.bbmodel`→`geo.json` — самописный скрипт, не
    официальный Blockbench-экспортёр), `relativeY<=0.85` эвристика клика
    по нижней панели.
  - **НЕ реализовано:** сам HUD-экран терминала (Познание/Мир/Я/
    Разблокировать), интеграция с системой Эйдоса.

### Что подтверждённо исправлено и работает (по состоянию на 25.07.2026)

- **Population balance у Formix:** `FORMIX_HIVE.supply` 0→5,
  `FORMIX_WARRIOR.population` 0→1. Стартовые worker'ы Formix = 3 (не 5) —
  сознательный игровой дисбаланс, подтверждён пользователем.
- **Ресурсы переименованы и получили кастомный арт от пользователя:**
  food→"Рис", wood→"Эфир", ore→"Версиформ" + своя иконка для Эйдоса
  (раздел 12.1 архива).
- **Прокачка персонажа (`playerprogression/`)** — персистентная, три
  статы (тело/разум/кулак) НЕ дают боевых бонусов, только
  future-разблокировку контента. Эйдос = реальный ваниль-XP игрока.
  Команда `/formix-stat <body|mind|fist|eidos|level> <значение> <игрок>`,
  OP-only. Дерево навыков/трата очков — **не реализовано**.
- **Граница застройки:** `BuildingUtils.BUILD_ZONE_RADIUS = 80`, серверно
  принудительная, общая для всех фракций.
- **Мгновенное зачисление ресурса** при личной добыче блока игроком —
  `ResourcesServerEvents.onPlayerBlockBreak`.
- Базовый MVP-пайплайн Formix (worker/warrior/Hive/конверсия/ресурсы) —
  не менялся с 21.07.2026.

### Не реализовано / отложено (явно, по словам пользователя)

- Рыбалка у юнитов-сборщиков — не начата.
- Реальная система траты скилл-поинтов прокачки персонажа, дерево
  навыков, "Эйдос-ферма"/мини-игры.
- Интеграция `playerprogression/` с `hero/` — сознательно раздельные
  системы.
- HUD-экран GeckoLib-терминала (Познание/Мир/Я/Разблокировать).

### Что временное/заглушка, ждёт реальных ассетов от пользователя

- 3D-модели worker/warrior — простые кубические placeholder'ы. Инструкция
  по замене — раздел 4.1 ниже.
- NBT-структура `FormixHive` — использует чужую схематику `town_centre`.
  Инструкция — раздел 4.2 ниже.
- mobhead-иконки (worker/warrior/здание) — программные заглушки.

**Единственный реальный юнит + здание в MVP** — сознательно минимальный
пайплайн (раздел 6 ниже). Второй юнит/здание — только по явной просьбе,
чеклист в разделе 8.

**Компиляция:** ни разу не проверялась мной вживую (нет Forge/GeckoLib
classpath в рабочей среде). Пользователь тестирует через IntelliJ +
Gradle и присылает логи ошибок. История исправленных багов — разделы
1.1–1.16 в архиве ниже. Это не гарантия отсутствия новых.

**Проверить в приоритете при следующем тесте пользователя:**
1. Полёт/noclip в Survival после ТРЕТЬЕГО фикса orthoview (раздел 1.17 архива) —
   оба сценария отдельно.
2. Активный баг population supply — ждёт лога по точной процедуре из
   раздела 1.16.
3. GeckoLib-терминал — компиляция, визуальная корректность модели в мире
   и в руке, клик по нижней панели.
4. Убрать все диагностические `[FORMIX-DEBUG...]` логи после диагностики.

**Известная отдельная проблема:** `build.gradle` в `.gitignore` — не
виден через `git clone`, только если пользователь явно пришлёт
текст/скриншот. Финальная рабочая версия (Curse Maven для GeckoLib)
описана в разделе 1.14 архива — стоит уточнить при следующей сессии,
применил ли пользователь её целиком.

**Инфраструктурное:** Claude Project для мода создан (репозиторий
доступен через `git clone` публичного GitHub-URL — не гарантированная
фича, проверять каждую сессию). GitHub-коннектор (MCP) — не предполагать
доступность без `tool_search` (раздел 10 архива).


---

## 1. Статус на данный момент: MVP-пайплайн реализован, требует ручной проверки в Gradle

Сборка **не проверялась реальным компилятором** — в песочнице, где пишется
код, нет Forge/Minecraft classpath (нет доступа к Maven-репозиториям Forge
через сеть). Весь код проверен только статическим анализом (сверка сигнатур
методов/конструкторов с существующими классами проекта). Перед использованием
обязательно собери проект (`./gradlew build` или через IDE) и почини то, что
вылезет — вероятность мелких ошибок (опечатка в импорте, забытый override)
есть, хоть и невысокая, т.к. каждая точка интеграции сверялась вручную.

### 1.1 Баги, найденные и исправленные после первой реальной сборки (19.07.2026)

Пользователь прислал реальный вывод IntelliJ Gradle build. Исправлено:

1. **`FormixWarriorRenderer.java` содержал класс `FormixWorkerRenderer`**
   (неверное имя класса/конструктора внутри правильно названного файла).
   Причина: файл создавался через `sed` копированием `FormixWorkerRenderer.java`
   с заменой `FormixWorkerModel→FormixWarriorModel` и
   `FormixWorkerUnit→FormixWarriorUnit`, но САМО имя класса
   `FormixWorkerRenderer`/конструктор не входило ни под один из этих
   паттернов замены (замена была `s/FormixWorkerModel/FormixWarriorModel/g`
   и `s/FormixWorkerUnit/FormixWarriorUnit/g` — обе не матчат ровно
   `FormixWorkerRenderer`). Это вызвало каскад ошибок: duplicate class,
   "public class in wrong file", cannot find symbol FormixWarriorRenderer в
   ClientModEvents, type mismatch в registerEntityRenderer. **Урок:** при
   генерации файла через sed-копирование всегда явно заменять ПОЛНОЕ имя
   класса как отдельный паттерн, не полагаться на то, что оно "подхватится"
   через замену вложенных подстрок типа Model/Unit.
2. **`FormixWorkerUnit.java` — лишний `@Override public boolean
   isConverting()`** — метод не переопределяет ничего ни в `Monster`/`Mob`,
   ни в реализуемых интерфейсах этого класса (`isConverting()` — это метод
   ванильного `Zombie`, никак не связанный с иерархией `Monster`, которую
   использует Formix). Похоже, попал в код по ошибке при копировании
   паттерна с VillagerUnit-подобного кода. Удалён — нигде не вызывался,
   удаление безопасно.

После этих правок оставшиеся warnings в логе (`ResourceLocation(String,String)
deprecated`, `setRenderLayer deprecated`) — **не мои баги**, они актуальны
для всего проекта целиком (Wraith/Windcaller/другие модели показывают те же
warnings), это накопленный technical debt от миграции Minecraft-версий,
чинить не нужно если явно не попросят.

### 1.2 Баг: фракция Formix не появлялась в экране выбора фракции (F12 лобби) — исправлено 19.07.2026

Пользователь сообщил, что в игре (экран выбора фракции перед стартом матча,
открывается клавишей, которую он назвал F12 — судя по коду это, скорее
всего, `MatchStartScreen`, отдельный полноценный UI-экран лобби, а не
`sandbox` режим, который был поправлен в первой итерации) фракция Formix не
отображалась среди плиток выбора, хотя Villagers/Monsters/Piglins были на
месте.

**Причина:** `matchstart/MatchStartScreen.java` содержит СОБСТВЕННЫЙ,
отдельный от `sandbox/SandboxClientEvents.java` жёстко закодированный
список фракций для отрисовки плиток выбора в лобби. Это отдельная точка
интеграции, которую я упустил при первичном аудите проекта — я нашёл и
поправил `PlayerServerEvents`/`PlayerServerboundPacket` (логика старта
матча) и `SandboxClientEvents` (песочница), но не увидел, что реальный,
основной UI выбора фракции для обычного RTS-матча рисуется в отдельном
классе `MatchStartScreen`.

**Что исправлено:**
1. `MatchStartScreen.java`: локальный массив `Faction[] order = {...}` внутри
   `renderSlotRow()` вынесен в поле класса `FACTION_TILE_ORDER` (добавлен
   `Faction.FORMIX`), используется в двух местах: цикл отрисовки плиток и
   расчёт ширины ряда фракций (`factionTotalW`), которые раньше жили
   раздельно и были рассинхронизированы (`FRAME_SIZE * 3` — магическое
   число под старые 3 фракции, не считая RANDOM). Теперь оба места
   вычисляются от `FACTION_TILE_ORDER.length`, что исключает повторение
   этого бага при добавлении новых фракций в будущем.
2. `startpos/StartPosClientEvents.java`: switch по `startPos.faction`,
   который задаёт превью здания-призрака при выборе стартовой позиции на
   карте, получил `case FORMIX -> Buildings.FORMIX_HIVE`. Без этого игрок,
   выбравший Formix, не видел бы превью здания при клике на карту (хотя
   само здание бы всё равно заспавнилось корректно при старте — это чисто
   UX-баг превью, не блокирующий).

**Урок на будущее:** при добавлении новой фракции нужно грепать
`switch.*faction`/`Faction\[\]`/`Faction\.VILLAGERS` по ВСЕМУ проекту, а не
только по тем файлам, что кажутся очевидными точками интеграции (player/
sandbox). Если добавляется что-то новое — сразу
`grep -rn "Faction.PIGLINS" src/` и проверять каждое совпадение, а не
полагаться на память о том, "где обычно бывают такие свитчи".

### 1.4 Баг: worker не конвертировался в warrior — исправлено 19.07.2026

**Причина:** `callToArmsGoal` создавался в `initialiseGoals()`, но никогда
не тикался. В отличие от большинства других goal-ов в этом проекте, которые
работают через стандартный Minecraft `goalSelector.addGoal(...)`,
`CallToArmsGoal`/`CallToArmsGoalFormix` — это goal особого рода: он
тикается **вручную** прямым вызовом `this.callToArmsGoal.tick()` внутри
`tick()` самого юнита (см. `VillagerUnit.tick()` строка ~384: `this.
callToArmsGoal.tick();` — стоит отдельной строкой рядом с `WorkerUnit.
tick(this)`, а НЕ добавлен в `goalSelector`). Я скопировал структуру
`initialiseGoals()`/`registerGoals()` из `VillagerUnit`, но упустил эту
единственную строку в `tick()`, поэтому объект goal существовал, но был
полностью мёртв — `isInRange()`/`convertToWarrior()` никогда не вызывались.

**Исправлено:** добавлена строка `this.callToArmsGoal.tick();` в
`FormixWorkerUnit.tick()`, сразу после `WorkerUnit.tick(this)` — 1:1 как в
оригинале.

**Урок на будущее:** goal-объекты в этом проекте бывают ДВУХ видов —
(1) обычные, добавленные через `goalSelector.addGoal(priority, goal)`
(тикаются автоматически движком Minecraft), и (2) "ручные" вспомогательные
goal-объекты вроде `CallToArmsGoal`, которые НЕ добавляются в
`goalSelector` и требуют явного `goal.tick()` внутри `tick()` юнита. При
копировании структуры юнита с полем типа `SomeGoal xxxGoal;` — всегда
проверять оба места: где создаётся (`initialiseGoals()`) И где тикается
(`registerGoals()` через addGoal ИЛИ `tick()` через прямой вызов), не
останавливаться после первого найденного места.

### 1.5 Баг: worker не подбирал дропы мяса (mutton/beef/etc.) с убитых животных — исправлено 19.07.2026

**Уточнение от пользователя:** речь шла именно про физические item-дропы,
падающие с убитых животных (mutton/beef/porkchop/etc.), не про блоки.

**Первая версия анализа (в этом файле) была неверной** — я решил, что эта
механика вообще не реализована в проекте. На самом деле она полностью
реализована и рабочая, я просто не проверил вызывающий код достаточно
глубоко в первый раз. Правильная картина:

- `resources/ResourceSources.getFromItem(Item)` резолвит предмет (BEEF,
  MUTTON, PORKCHOP, RABBIT, и т.д.) в `ResourceSource`/`ResourceName.FOOD`.
- `unit/UnitServerEvents.onDropItem(LivingDropsEvent)` — глобальный
  Forge-обработчик: когда `WorkerUnit` убивает "huntable" животное, дроп
  ОТМЕНЯЕТСЯ (`evt.setCanceled(true)`) и вместо ваниль-дропа юнит получает
  гарантированный набор еды напрямую (`ResourceSources.
  getFoodItemsFromAnimal(animal)` → `unit.getItems().add(...)`) — то есть
  killing an animal as a worker instantly banks food, no separate pickup
  step needed for that specific kill.
- Для дропов, которые всё-таки падают на землю (например животное убито НЕ
  worker'ом, или дроп от чего-то другого) — их подбирает
  `unit/interfaces/Unit.java` → `checkAndPickupResources(Unit)`, вызываемый
  каждый тик из `Unit.tick(unit)` (которое я уже вызываю в обоих
  Formix-юнитах). Этот метод сканирует `ItemEntity` в радиусе 1 блока и
  добавляет их в `unit.getItems()` через `ResourceSources.getFromItem(...)`.

**Реальная причина бага:** ОБА этих механизма (`onDropItem` мгновенный
дроп-в-руки И `checkAndPickupResources` подбор с земли) требуют
`((Mob) unit).canPickUpLoot() == true` как обязательное условие — это
первая же проверка в обоих местах. Я по ошибке поставил
`this.setCanPickUpLoot(false)` в обоих Formix-юнитах (`FormixWorkerUnit` и
`FormixWarriorUnit`), перепутав паттерн с чем-то другим. **Абсолютно все**
остальные юниты в проекте (проверено `grep`, 27 файлов) ставят `true` —
это универсальный паттерн, не зависящий от фракции.

**Исправлено:** `setCanPickUpLoot(false)` → `setCanPickUpLoot(true)` в
обоих файлах (`FormixWorkerUnit.java`, `FormixWarriorUnit.java`). Пустой
override `protected void pickUpItem(ItemEntity) {}` остаётся как есть (это
намеренно — блокирует ТОЛЬКО ваниль-логику визуального добавления предмета
в руку моба, не мешает подбору ресурсов, который идёт отдельным путём
через `checkAndPickupResources`/`onDropItem`).

**Важный нюанс:** мгновенный дроп-в-руки при убийстве (`onDropItem`)
требует `instanceof WorkerUnit` — `FormixWarriorUnit` этим интерфейсом
НЕ реализован (сознательно, см. раздел 5 — warrior не собирает ресурсы).
Значит warrior, убивший животное, НЕ получит мгновенный гарантированный
дроп еды тем же путём, что worker — животное просто даст обычный ваниль-
дроп на землю, который `checkAndPickupResources` подберёт (т.к. теперь
`canPickUpLoot() == true`), но без бонусов типа гарантированного количества
или `incrementHunterExp()`. Это соответствует поведению `MilitiaUnit`
(тоже не `WorkerUnit`) — то есть Formix ведёт себя симметрично villager-
паттерну, разницы в поведении между фракциями нет.

**Урок на будущее:** при копировании юнита из чужого паттерна — свериться
`grep -n "setCanPickUpLoot" оригинальный_файл.java` для КАЖДОГО значения
true/false, а не полагаться на память о том, "это наверное отключено у
воркеров чтобы не подбирали случайный мусор" (интуитивно кажется логичным,
но оказывается неверным — в этом проекте подбор всегда включен, просто
физическое добавление в РУКУ (визуал) отдельно заблокировано через
`pickUpItem()` override).

### 1.6 Фича: счётчик ресурсов (food/wood/ore), видимый поверх любого экрана — добавлено 19.07.2026

Пользователь хотел видеть свои ресурсы (дерево/еда/руда) в левом верхнем
углу при открытии обычного Minecraft-инвентаря, не переключаясь на
полноэкранный RTS-режим (в этом проекте называется "orthoview"/"topdown
view", открывается функциональной клавишей — то, что пользователь называл
"F12"; см. `keybinds/Keybindings.java` для точной привязки, по умолчанию
это `OrthoviewClientEvents`-переключатель).

**Важный нюанс архитектуры, обнаруженный по пути:** пока orthoview активен,
`player/PlayerClientEvents.onScreenOpen` СОЗНАТЕЛЬНО блокирует открытие
обычного Minecraft-инвентаря (`evt.setCanceled(true)` для
`InventoryScreen`/`CreativeModeInventoryScreen`) — то есть в самом RTS-виде
привычного инвентаря с сеткой предметов не существует. Значит запрос
пользователя относится к моменту, когда игрок НЕ в orthoview (обычная
Minecraft-игра от первого/третьего лица, но матч уже идёт) и открывает
обычный инвентарь клавишей E.

**Реализация:** новый метод `hud/HudClientEvents.
onDrawScreenResourcesOverlay(ScreenEvent.Render.Post)` — отдельный,
независимый от существующего `onDrawScreen` (который жёстко привязан к
`instanceof TopdownGui` и поэтому не годился для этой задачи напрямую).
Новый метод:
- Пропускает `TopdownGui` (там уже есть полная панель ресурсов через
  старый `onDrawScreen`, не дублируем).
- Показывается только пока `PlayerClientEvents.isRTSPlayer()` истинно
  (т.е. игрок реально участвует в активном RTS-матче) — не отвлекает людей
  вне матча / в ванильном одиночном Minecraft.
- Рисует 3 компактные иконки (food/wood/ore) с числами в левом верхнем
  углу (`startX=4, startY=4`), переиспользуя существующие рендер-хелперы
  `util/MyRenderer.renderFrameWithBg/renderIconFrameWithBg/renderIcon` и
  те же текстуры/цвета, что использует полная RTS-панель ресурсов
  (`textures/icons/items/wheat.png` и т.д.), чтобы визуально не выглядеть
  чужеродно.
- Это ПРОСТОЙ read-only виджет — без world-position подсчёта "сколько
  worker'ов назначено на этот ресурс" (в отличие от полной панели в
  `onDrawScreen`, там это есть) — не нужно для компактного индикатора, и
  такой подсчёт был бы избыточно дорогим для отрисовки на каждом кадре
  поверх произвольных экранов.

**Не является Formix-специфичной правкой** — это общая QoL-фича проекта,
работает для всех фракций одинаково (показывает ресурсы ТЕКУЩЕГО игрока,
не завязано на конкретную фракцию).

**Если нужно доработать:** позиция (`startX`/`startY`), порядок ресурсов
(`resourceOrder` массив), видимость (сейчас показывается на любом экране,
включая chat/pause — если нужно ограничить конкретными экранами, добавь
`evt.getScreen() instanceof XxxScreen` проверку в начале метода).

### 1.7 Иконки: заменены плоские заглушки на узнаваемые силуэты — добавлено 19.07.2026

Пользователь хотел сразу понимать, какого юнита/здание он выбирает, глядя
на иконку — прежние плоские заливки (сплошной амбер/тёмно-красный
квадрат) ничего не сообщали. **Настоящих арт-ассетов от пользователя (из
Blockbench) ещё нет** — пользователь явно попросил улучшить программные
заглушки, не дожидаясь реального арта.

**Что сделано:**
- `textures/mobheads/formix_worker.png` — перерисован программно
  (Python/Pillow) как стилизованная инсектоид-голова: амбер/охра-хитин,
  тёмные фасеточные глаза, небольшие жвала, антенны, сегментированные
  линии панциря. Спокойный, не агрессивный силуэт.
- `textures/mobheads/formix_warrior.png` — НОВЫЙ файл (раньше не
  существовал, warrior не имел отдельной mobhead-иконки, т.к. не
  производится напрямую через `Prod`-класс). Тёмно-красный хитин,
  светящиеся жёлто-янтарные "злые" глаза, крупные острые жвала, шипы на
  голове — визуально очевидно агрессивнее/крупнее worker'а при взгляде
  рядом. Пока НЕ подключен ни в одном Java-файле (см. ниже, почему).
- `building/buildings/formix/FormixHive.java` — иконка/portraitBlock
  здания сменены с нейтрального `Blocks.WARPED_NYLIUM` (никак не связан с
  темой фракции, был чисто временным выбором) на `Blocks.HONEYCOMB_BLOCK`
  — по духу ближе к theme насекомых/улья, которую называл пользователь.
  Заменено в ОБОИХ местах, где использовалась иконка (конструктор + метод
  `getBuildButton()` — эти две точки не были синхронизированы через общую
  константу, легко забыть одну при следующей правке — см. TODO ниже).

**Почему `formix_warrior.png` создан, но нигде не используется в коде:**
mobhead-иконки в этом проекте применяются ТОЛЬКО в `XxxProd` классах
(кнопки производства в здании) — проверено `grep` по всем использованиям
`mobheads/*.png` в Java-коде. Warrior не имеет `FormixWarriorProd` (по
дизайну — получается только конверсией из worker'а, не производится
напрямую), поэтому у него физически нет UI-слота, где такая иконка была бы
показана. Портрет уже ЖИВОГО выбранного юнита (worker или warrior) в игре
рисуется не через mobhead-иконку, а через настоящий 3D-рендер модели юнита
(`hud/PortraitRendererUnit.java` → `EntityRenderDispatcher`) — то есть он
automatически покажет актуальную модель (сейчас placeholder-куб, после
замены — твою Blockbench-модель) без какой-либо доп. настройки текстуры.
Файл `formix_warrior.png` подготовлен заранее на случай, если в будущем
появится место, где нужна плоская иконка warrior'а (например если добавишь
отдельную кнопку способности "показать доступных warrior'ов" или что-то
подобное) — использовать оттуда:
`ResourceLocation.fromNamespaceAndPath(ReignOfNether.MOD_ID,
"textures/mobheads/formix_warrior.png")`.

**TODO для консистентности (не критично, мелочь):** `FormixHive.icon` и
иконка в `getBuildButton()` — это два раздельных литерала строки, а не
одна переменная. Если в будущем ещё раз меняешь иконку здания — не забудь
поменять оба места (сейчас оба указывают на `honeycomb_block.png`,
синхронизированы вручную).

**Когда появятся реальные Blockbench-ассеты** — эти программные mobhead-
иконки тоже стоит заменить на настоящий арт (обычно это отдельный,
более крупный/детальный портрет, а не превью 3D-модели) для консистентности
со стилем остальных фракций (`grunt.png`/`headhunter.png` и т.д. — это
нарисованные вручную иконки, не автогенерированные из 3D-модели).

### 1.8 Ресурсы в инвентаре + RTS-режим в Survival/Adventure — 21.07.2026

**Часть A — счётчик ресурсов, переделан под референс пользователя.**
Пользователь прислал скриншот другого мода/пака: 3 вертикальные плашки
(иконка + число, широкая рамка с подсветкой), видимые ТОЛЬКО в инвентаре, а
не на весь экран как было раньше. Полностью переписан
`onDrawScreenResourcesOverlay` в `hud/HudClientEvents.java`:
- Условие видимости сужено с "любой экран" до строго
  `instanceof InventoryScreen || instanceof CreativeModeInventoryScreen`
  (оба импортированы явно).
- Layout сменён с горизонтального ряда мелких иконок на вертикальный
  список широких плашек (`rowWidth=150, rowHeight=34`, с отступом
  `rowSpacing=6` между строками), каждая через `MyRenderer.
  renderFrameWithBg(...)` — тот же готовый "unit_frame" стиль рамки,
  что используется для панелей юнитов в остальном HUD, для визуальной
  консистентности с общим стилем мода.
- Не показывает "/max" (в отличие от референс-скриншота), т.к. в этом
  моде у ресурсов нет фиксированного капа на 1000 — это архитектурное
  отличие оригинала, не то, что можно скопировать буквально.

**Часть B — RTS-режим (orthoview) разрешён в Survival и Adventure, не
только Creative.** Раньше `orthoview/OrthoviewClientEvents.java` жёстко
блокировал вход в RTS top-down вид в этих двух режимах (показывал ошибку
`hud.orthoview.reignofnether.ortho_error` и не давал включиться,
`tryToToggleEnable()`), и ДОПОЛНИТЕЛЬНО принудительно выключал orthoview
каждый тик, если игрок каким-то образом оказался в нём при Adventure/
Survival (в `onClientTick`-подобном методе). Оба ограничения убраны.

**Настоящая техническая причина ограничения (не просто "было лень
разрешить"):** включение orthoview телепортирует игрока на большую высоту
(`orthoviewPlayerBaseY = 100`) и держит его там для вида "сверху вниз".
В Creative это не проблема — creative-игроки летают по умолчанию. В
Survival/Adventure без специальной noclip/flight-логики игрок либо
проваливался бы в свободное падение сразу после телепорта, либо получал
fall damage при выходе из режима. Эта логика ФИЗИЧЕСКИ УЖЕ БЫЛА написана
автором мода — `mixin/PlayerMixin.java`, метод `tick()` — но была
полностью закомментирована и жёстко ограничена условием
`player.isCreative()`, с явным комментарием в коде: `"only needed if
orthoview players are in CREATIVE mode"`. Похоже автор либо тестировал
и что-то не заработало для non-Creative, либо просто не успел довести до
конца эту часть — история/git-лог для точного ответа недоступны.

**Что сделано:** раскомментирован `tick()` метод в `PlayerMixin.java`,
убрано условие `player.isCreative()` — теперь `noPhysics=true` и
`flying=true` принудительно ставятся для ЛЮБОГО игрока, находящегося в
orthoview (через `PlayerServerEvents.orthoviewPlayers` на сервере или
`OrthoviewClientEvents.isEnabled()` локально на клиенте), независимо от
геймтайпа. Класс `PlayerMixin` уже был зарегистрирован в
`reignofnether.mixins.json` (просто был no-op из-за закомментированного
тела) — не нужно ничего менять в конфигурации мода, только сам код.

**ВАЖНЫЙ РИСК, требующий проверки в реальной игре (я не могу это
протестировать без живого Minecraft-клиента):**
- Закомментированный оригинал имел комментарий: `"tick() naturally
  reverses all of this so no need for reversing it here when leaving
  orthoView"` — это предположение полагается на то, что ваниль-логика
  Minecraft сама сбрасывает `flying`/`noPhysics` при выходе из полёта. Это
  логично для Creative (там `flying` и так ожидаемое состояние), но
  **не гарантированно так же чисто работает для Survival/Adventure** —
  есть риск, что игрок останется "залипшим" в режиме полёта/без
  столкновений после выхода из orthoview в этих режимах, если ваниль не
  сбрасывает `getAbilities().flying` автоматически для них.
- **Если это произойдёт** (игрок продолжает летать/проваливается сквозь
  блоки после выхода из RTS-вида в Survival/Adventure) — быстрый фикс:
  в `OrthoviewClientEvents.toggleEnable()`, в ветке `else` (когда
  `enabled` становится `false`, т.е. выход из режима), явно добавить
  сброс `MC.player.getAbilities().flying = false;
  MC.player.onUpdateAbilities();` на клиенте, и аналогичный явный сброс
  `noPhysics`/`flying` на сервере в `PlayerServerEvents.
  disableOrthoview(int id)`. Не добавлено заранее, чтобы не чинить то,
  что возможно не сломано — сначала нужно реальное подтверждение бага в
  игре.
- Другой возможный риск: fall damage при телепортации ВНИЗ (например если
  сервер уменьшает `orthoviewPlayerBaseY` относительно текущей позиции
  игрока, или landscape под ним ниже базовой RTS-высоты) — раз
  `noPhysics=true` действует, пока `isEnabled()`/игрок в
  `orthoviewPlayers`, сам процесс нахождения в режиме должен быть
  безопасен, риск именно в моменте ВЫХОДА.
- Проверить также: голод/мобы-атаки в Survival, пока игрок находится в
  RTS-виде (сам orthoview не даёт неуязвимости, только noclip/flight —
  игрок физически стоит/висит в воздухе на `orthoviewPlayerBaseY`, куда
  агрессивные мобы не должны доставать, но это стоит перепроверить
  визуально).

**Если после тестов окажется, что риски выше некритичны** — эта фича
полностью рабочая end-to-end (сама блокировка входа снята, физика полёта
обеспечена для всех режимов). Если найдутся баги — присылай точное
описание (что произошло при выходе из orthoview в survival) и я поправлю
точечно, не откатывая всю фичу.

### 1.9 Лимит хранения ресурсов (1000) + уменьшение размера HUD-плашек — 21.07.2026

Пользователь прислал скриншот нового вида плашек ресурсов (после правки
1.8) — визуально они оказались слишком широкими (`rowWidth=150` для
короткого текста типа "500"). Плюс запрошен реальный (не только визуальный)
лимит хранения ресурсов по образцу скриншота "150/1000".

**Важный архитектурный факт, выясненный перед реализацией:** до этой
правки в моде НЕ было понятия лимита хранения ресурсов вообще — `Resources`
(в `resources/Resources.java`) хранил `food/wood/ore` как простые
неограниченные `int`. Это подтверждено пользователем явно как желаемое
геймплейное изменение (не просто текст в HUD), после уточняющего вопроса.

**Реализация — централизованный clamp внутри `Resources`, не в местах
вызова:**
- `MAX_RESOURCE_AMOUNT = 1000` — новая public static константа в
  `Resources.java`.
- `changeInstantly()` (server-side начисление) и `changeOverTime()`
  (client-side анимация счётчика) теперь клэмпят результат через приватные
  `clampTotals()`/`clampToAddFields()`. Только ДВА метода реально меняют
  ресурсы во всём проекте (`grep` подтвердил — 3 места вызова, все через
  эти два метода), поэтому клэмп в самом классе `Resources` защищает
  ВЕСЬ проект автоматически, без необходимости трогать вызывающий код
  (`ResourcesServerEvents`, `ResourcesClientEvents`, и любой будущий код).
- Траты (отрицательные изменения, покупка юнитов/зданий) НЕ блокируются
  клэмпом — `Math.min(x, MAX)` только режет положительный прирост сверху,
  никогда не мешает тратить то, что уже есть.
- `tick()` (постепенный "тающий" анимационный дренаж `foodToAdd` → `food`
  для HUD) не требует отдельного клэмпа — он не создаёт новое количество,
  только переносит уже клэмпнутое значение из `foodToAdd`, поэтому
  арифметически не может превысить лимит сам по себе.

**КРИТИЧЕСКИ ВАЖНОЕ ИСКЛЮЧЕНИЕ — sandbox-игроки:** обнаружено, что
sandbox-режим стартует игроков с `STARTING_FOOD_SANDBOX = 999999` (и
аналогично wood/ore) — специально безлимитные ресурсы для свободного
тестирования построек/юнитов без ограничений. Если бы clamp применялся
безусловно, ПЕРВАЯ ЖЕ трата в sandbox обрезала бы `999999` до `1000`
(т.к. `changeInstantly(-цена)` вызвал бы `clampTotals()`, а
`Math.min(999999 - цена, 1000) = 1000`), сломав весь смысл sandbox-режима.
Поэтому оба clamp-метода начинаются с проверки
`SandboxServer.isSandboxPlayer(this.ownerName)` — если true, клэмп
полностью пропускается, sandbox остаётся безлимитным как и было.
(Небольшая архитектурная деталь: `isSandboxPlayer` читает server-side
список `PlayerServerEvents.rtsPlayers`, значит на выделенном/dedicated
сервере вызов этого метода из клиентского `changeOverTime()` может не
видеть актуальный список — на practice это не критично, т.к. авторитетное
значение всё равно придёт с сервера следующим тиком, но если когда-то
понадобится 100% чистое разделение client/server логики здесь — стоит
передавать флаг "является ли sandbox" явным параметром вместо обращения к
серверному синглтону изнутри модели данных.)

**HUD-плашки (только визуал):** `rowWidth` уменьшен со 150 до 108,
`rowHeight` с 34 до 30, `iconSize` с 18 до 16 — под реальный размер текста
"1000 / 1000" (худший случай по длине). Текст теперь показывает
`"{значение} / {MAX_RESOURCE_AMOUNT}"` вместо голого числа.

**Новый файл:** `PROJECT_NOTES/QUICK_REFERENCE.md` — практическая шпаргалка
по типовым правкам (текстуры/модели/юниты/здания/баланс) плюс ответ про
git-синхронизацию (на 21.07.2026 казалось, что нет прямого доступа к
GitHub — **это устарело**, см. раздел 0 выше и раздел 10 архива ниже: с
21-25.07.2026 подтверждено, что `git clone` публичного репозитория
пользователя реально работает). Читать вместе с этим файлом, но не
дублировать информацию между ними — здесь история решений по Formix, там
— общие процедуры на будущее.

### 1.3 Заодно исправлено: debug "start RTS" кнопки и связанная инфраструктура

При широком грепе `Faction.PIGLINS` (после находки 1.2) заодно закрыты ещё
несколько мест, которые не блокировали игру, но были неполными:

- `unit/UnitAction.java` — добавлен `STARTRTS_FORMIX` (отдельный от
  `PlayerAction.START_RTS_FORMIX` enum, используется debug-кнопками
  быстрого старта RTS через клик по миру).
- `hud/buttons/StartButtons.java` — добавлена `formixStartButton` (копия
  `piglinStartButton` с иконкой `formix_worker.png`).
- `hud/HudClientEvents.java` — добавлен рендеринг `formixStartButton` на
  позиции `ICON_SIZE * 3` (НЕ `* 2`, т.к. та позиция уже занята
  `gamemodeButton` — если добавляешь ещё одну debug-кнопку в этот ряд в
  будущем, проверь все текущие offset'ы вокруг строки ~1560-1600 первым
  делом, они не идут строго по чистой арифметической прогрессии).
- `unit/UnitClientEvents.java` — оба блока (`startrts`/`sandbox_spawn`
  action-обработчики) получили `case STARTRTS_FORMIX ->
  startRTS(Faction.FORMIX, ...)`.
- `cursor/CursorClientEvents.java` — оба `List.of(STARTRTS_...)`
  фильтра (используются для распознавания "это клик = запуск RTS")
  получили `STARTRTS_FORMIX`.
- `en_us.json` — `hud.startbuttons.formix.reignofnether.first/second`.

**Сознательно НЕ тронуто:** `hud/TitleClientEvents.java` — случайный выбор
фракции для декоративной панорамы главного меню. Formix туда специально НЕ
добавлен, т.к. панорамные skybox-текстуры (`textures/gui/title/background/
formix/panorama/*`) не существуют — добавление привело бы к missing texture
или чёрному фону в меню. Добавь Formix в `getRandomFaction()`/
`getNewRandomFaction()`/`getBackgroundLocation()`-подобный switch (строка
~30 того файла) только после того, как появятся панорамные текстуры.

### 1.10 Population: Warrior не считался в занятом населении (исправлено) + Hive supply=0 (диагностика начата, НЕ завершена) — 25.07.2026

**Часть A — исправлено.** Пользователь сообщил: при конверсии worker→warrior
юнит "пропадал" из занятого населения (счётчик X/Y уменьшался), при
обратной конверсии в worker снова появлялся. Причина найдена сразу:
`ReignOfNetherCommonConfigs.UnitCosts.FORMIX_WARRIOR` был объявлен с
`population=0` (`ResourceCostConfigEntry.Unit(food, wood, ore, seconds,
population, ...)`), тогда как `FORMIX_WORKER` имел `population=1`.
`UnitServerEvents.getCurrentPopulation()` суммирует `unit.getCost().
population` по всем живым юнитам заново при каждом подсчёте — конверсия
никак не трогает population вручную, значение просто берётся из текущего
конфига юнита. Исправлено: `FORMIX_WARRIOR` теперь `population=1`.

**Часть B — НЕ завершено, начата диагностика.** Отдельная жалоба:
`FormixHive` после постройки не даёт population supply, счётчик всегда
0/0 (при том что `BuildingCosts.FORMIX_HIVE` в конфиге содержит
`supply=5`, ещё с сессии от 23.07 — раздел ниже "23.07.2026 (сессия про
баланс population)"). Прошёл ВСЮ цепочку статически, не нашёл разницы с
рабочим `TownCentre`:
1. `ReignOfNetherCommonConfigs.BuildingCosts.FORMIX_HIVE` — `supply=5`,
   4-й параметр `Building(food, wood, ore, supply, ...)`, синтаксически
   верно.
2. `ResourceCosts.FORMIX_HIVE.bakeValues(ReignOfNetherCommonConfigs.
   BuildingCosts.FORMIX_HIVE)` — вызывается в `ResourceCosts.
   deferredLoadResourceCosts()`, которая в свою очередь вызывается и на
   сервере (`ReignOfNether.java`), и на клиенте (`ConfigClientEvents.java`)
   — идентично другим зданиям, не Formix-специфично.
3. `ResourceCost.bakeValues()` читает `rcce.getPopulation()` →
   `this.POPULATION.get()` (ForgeConfigSpec-значение) — стандартный,
   работающий у всех зданий путь.
4. `FormixHive.cost = ResourceCosts.FORMIX_HIVE` — `public final static`
   поле, тот же паттерн, что `TownCentre.cost = ResourceCosts.
   TOWN_CENTRE` (проверено построчным сравнением обоих файлов).
5. `Building(String structureName, ResourceCost cost, boolean isCapitol)`
   конструктор — просто `this.cost = cost;` (присваивание ссылки, не
   копия) — значит поздний `bakeValues()` обязан быть виден через
   `FormixHive.cost.population` тоже, т.к. это тот же объект в памяти.
6. `FormixHive()` конструктор — `super(structureName, cost, true)`,
   `isCapitol=true` передан верно (важно, т.к. `isCapitol` используется в
   других местах — граница застройки и т.д., раздел "22.07.2026,
   продолжение" — но не влияет на `getTotalPopulationSupply()` напрямую,
   проверено, что этот метод его не читает).
7. `Buildings.FORMIX_HIVE = register(..., new FormixHive())` — стандартная
   регистрация, идентична другим зданиям.
8. `BuildingServerEvents.getTotalPopulationSupply(ownerName)` — цикл по
   `buildings`, условие `building.ownerName.equals(ownerName) &&
   building.isBuilt`, суммирует `building.getBuilding().cost.population`
   — НЕТ никакой фильтрации по типу/классу здания, работает одинаково для
   любого `BuildingPlacement`.
9. `BuildingPlacement.isBuilt = true` выставляется в общем, не
   Formix-специфичном коде (сработавший `discardStructureVoidBlocks()`/
   завершение стройки) — не должно зависеть от того, что `FormixHive`
   временно использует чужую NBT-структуру `town_centre` (сам механизм
   постройки/структур не завязан на семантику "что это за здание", только
   на геометрию блоков).

**Ни на одном из этих 9 шагов не найдено расхождения с рабочим
`TownCentre`.** Вывод: причина либо в чём-то, не видимом статическим
анализом (реальный порядок событий Forge при загрузке, специфика
конкретной игровой сессии пользователя), либо в файле/месте, которое ещё
не проверено (например `ResourceCostsClientboundPacket`/сетевая
синхронизация supply на клиент — не проверялось в этой сессии, стоит
посмотреть в следующей, если лог не даст ответа сразу).

**Временный диагностический код добавлен** — `System.out.println` внутри
`BuildingServerEvents.getTotalPopulationSupply()`, помечен комментарием
`TEMP DEBUG (Formix population supply investigation, remove after
diagnosis)`. Выводит для каждого здания текущего игрока: класс, `isBuilt`,
`cost.population`, `ownerName`, с префиксом `[FORMIX-DEBUG]` для лёгкого
поиска в консоли. **Ждём от пользователя лог после теста** (построить
Hive, зайти в игру, глянуть консоль сервера/Run-панель IntelliJ).

**ОБЯЗАТЕЛЬНО удалить `System.out.println`-блок после получения
диагностики и исправления настоящей причины** — это не должно остаться в
финальной версии кода, это чисто временный инструмент этой сессии.

**Урок на будущее:** когда статический анализ (сверка кода 1:1 с рабочим
референсом) не находит расхождений, но баг реально воспроизводится —
не стоит продолжать бесконечно искать "ещё одну гипотезу" вслепую;
эффективнее сразу добавить точечный диагностический лог и получить
реальные runtime-значения, чем перебирать теоретические причины одну за
другой. Диагностика должна быть явно помечена как временная (комментарий
+ единообразный префикс в логе), чтобы не забыть убрать её после
  использования.

### 1.11 Population display: разные значения занятого населения для Worker/Warrior в один момент (диагностика начата) — 25.07.2026, вечер

Продолжение расследования из 1.10. Пользователь прислал два скриншота
ОДНОГО И ТОГО ЖЕ игрового момента — на одном выбран `Formix Warrior`
(население 0/0), на другом `Formix Worker` (население 3/0). Разобрано:
- Общие ресурсы (рис/эфир/версиформ) ОДИНАКОВЫ на обоих скриншотах — это
  доказывает, что `selPlayerName` (владелец выбранного юнита,
  `HudClientEvents`) резолвится в одного и того же игрока в обоих
  случаях, иначе общие ресурсы тоже бы отличались.
- Значит разница `0` vs `3` — не в определении владельца, а в самом
  подсчёте `UnitClientEvents.getCurrentPopulation(selPlayerName)`,
  вызванном с ОДНИМ И ТЕМ ЖЕ аргументом, но дающем разный результат в
  разные моменты рендера HUD.

**Добавлен throttled (раз в секунду) диагностический лог** —
`UnitClientEvents.getCurrentPopulation()`, префикс
`[FORMIX-DEBUG-CLIENT]`. Выводит `playerName`, итоговый `result`, полный
список `allUnits` с `owner`/`costPopulation` каждого. Этот лог, в отличие
от лога для Hive supply (см. 1.12 ниже), стоит в ПРАВИЛЬНОМ месте с
самого начала — `UnitClientEvents` это единственный класс с методом
`getCurrentPopulation`, вызываемым из `HudClientEvents`, дублирующей
Server-версии для этого конкретного метода нет (в отличие от
`getTotalPopulationSupply`, см. 1.12).

**Статус:** лог добавлен, но ждёт результата теста от пользователя.

### 1.12 Найдена причина, почему серверный диагностический лог для Hive supply никогда не срабатывал — 25.07.2026, поздний вечер

Пользователь прислал полный `run/logs/latest.log` после теста (построил
`FormixHive`, дождался полной постройки, кликнул по зданию/юниту в
пределах времени лога) — в логе НЕТ НИ ОДНОЙ строки `[FORMIX-DEBUG]`
(лог из раздела 1.10), при этом подтверждено: здание построено, клик по
юниту/зданию точно был сделан вовремя.

**Настоящая причина — архитектурная, не связана с самим population-багом
напрямую.** В проекте есть ДВЕ ОТДЕЛЬНЫЕ реализации
`getTotalPopulationSupply()` с одинаковым именем в разных классах:
- `BuildingServerEvents.getTotalPopulationSupply(String ownerName)` —
  серверная, использует серверный список `buildings`. Используется в
  `ProductionItem`/при проверках возможности постройки на сервере — НЕ
  в отрисовке HUD.
- `BuildingClientEvents.getTotalPopulationSupply(String playerName)` —
  ОТДЕЛЬНАЯ клиентская, свой список `buildings` (синхронизированный по
  сети). **Именно эта версия вызывается из `HudClientEvents.java`
  (строка ~1164), то есть именно её результат видит игрок в панели
  ресурсов.**

Диагностический лог из раздела 1.10 был по ошибке добавлен в СЕРВЕРНУЮ
версию — решение основывалось на совпадении имени метода и идентичной
логике, без явной проверки (`grep`), какой конкретно класс вызывается
из `HudClientEvents`. Отсюда полное отсутствие лога — метод, который
логировался, физически не вызывался в контексте отрисовки HUD.

**Исправлено:**
- Убран бесполезный лог из `BuildingServerEvents.
  getTotalPopulationSupply()`, метод возвращён к чистому оригинальному
  виду.
- Добавлен НОВЫЙ throttled лог (раз в секунду) в `BuildingClientEvents.
  getTotalPopulationSupply()` — правильное место. Префикс
  `[FORMIX-DEBUG-CLIENT-SUPPLY]` (намеренно отличается от старого
  `[FORMIX-DEBUG]`, чтобы не создавать путаницы, если пользователь по
  привычке будет искать старый префикс). Выводит `playerName`, `result`,
  полный список известных клиенту зданий с `owner`/`isBuilt`/
  `costPopulation`.

**Оба диагностических лога сейчас активны и оба в правильных местах:**
1. `[FORMIX-DEBUG-CLIENT-SUPPLY]` (`BuildingClientEvents.
   getTotalPopulationSupply`) — про Hive supply=0.
2. `[FORMIX-DEBUG-CLIENT]` (`UnitClientEvents.getCurrentPopulation`) —
   про занятое население, разное при разных выбранных юнитах (раздел
   1.11).

**Также уточнено, как пользователю искать лог надёжно:** панель Run в
IntelliJ иногда не открывает встроенный поиск по `Ctrl+F` корректно —
надёжнее открыть файл `run/logs/latest.log` напрямую (он в дереве
проекта) и искать текст там любым текстовым редактором/поиском файла.

**Следующий шаг:** получить от пользователя `run/logs/latest.log` (или
текст с обоими префиксами) после НОВОГО теста с этой версией кода. После
получения — исправить оба бага и обязательно убрать оба лога.

**Урок на будущее (важно для этого проекта конкретно):** в этом Forge-
моде одноимённые методы часто существуют раздельно в `XxxServerEvents` и
`XxxClientEvents` классах (стандартный паттерн клиент-серверного разделения
состояния) — при добавлении диагностики в такой метод ОБЯЗАТЕЛЬНО сначала
`grep` по вызывающему UI/HUD-коду, чтобы подтвердить, какая именно версия
(Client или Server) реально задействована в конкретном отображаемом
сценарии, прежде чем логировать не тот метод и тратить целый цикл
тестирования впустую.

### 1.13 Блок-терминал control station: первое подключение GeckoLib в проект — 26.07.2026, вечер

Пользователь прислал `control_station.bbmodel`/`control_station_diactive.bbmodel`
(активная/неактивная 3D-модель терминала) + 2 скриншота референса игрового
UI терминала (три раздела "Познание"/"Мир"/"Я", кнопка "Разблокировать за
10 [иконка Эйдоса]"). Запрос: блок + модель, и переход в RTS-режим по клику
на нижнюю панель модели вместо клавиши F12 — БЕЗ самого HUD-экрана
терминала (это явно отложено на будущую сессию).

**Ключевое техническое открытие: модели в формате GeckoLib, а не ваниль.**
`.bbmodel` содержит `"model_format": "geckolib_model"` и
`"geckolib_model_type": "Block"` — это отдельный Blockbench-пайплайн
экспорта, несовместимый с `HierarchicalModel`/`ModelPart`, которым
пользуются ВСЕ существующие модели юнитов в проекте (`WraithModel` и
т.д.). `grep` по всему репозиторию и по `mods.toml` подтвердил: GeckoLib
нигде не подключён. Дал пользователю выбор (переэкспортировать в
ваниль-формат / добавить GeckoLib / временная заглушка) — выбран GeckoLib.

**`build.gradle` физически недоступен ассистенту.** `git ls-tree`
подтвердил, что `build.gradle` в `.gitignore` пользователя — значит через
`git clone` (см. раздел 10 про GitHub-доступ) файл не виден вообще, даже
несмотря на то что репозиторий публичный. Это первый зафиксированный
случай, когда доступность репозитория НЕ означает доступность конкретного
файла в нём. Пользователю дана точная инструкция для ручного добавления
(`geckolib-forge-1.20.1:4.4.4`, репозиторий Cloudsmith) — версия выбрана
по линейке 4.4.x как совместимая со старым `loaderVersion=[47,)` в
`mods.toml`, НЕ проверено вживую (нет сети до GeckoLib Maven в песочнице).

**Расхождение между паттерном проекта и официальной документацией
GeckoLib4, найденное и исправленное ДО отправки кода:** единственный
пример кастомного блока с BlockEntity-рендером в проекте
(`GarrisonEntryBlock`) использует `getRenderShape()=RenderShape.INVISIBLE`.
Сначала по инерции скопировал этот паттерн для `FormixControlStationBlock`,
но официальная GeckoLib4-вики явно требует `RenderShape.ENTITYBLOCK_ANIMATED`
для блоков, рендерящихся через `GeoBlockRenderer` — `INVISIBLE` тут неверен.
Пойман и исправлен веб-поиском официальной документации ПЕРЕД отправкой
кода. **Урок:** при работе с новой для проекта библиотекой не
экстраполировать паттерны существующего кода автоматически на новую
технологию, даже если задача выглядит похоже — сверяться с официальной
документацией именно этой библиотеки.

**Габариты модели: пришлось уточнить у пользователя.** Крайние координаты
элементов в `.bbmodel` (X от -70 до 30.5, Y от 0 до 96) намекали на модель
шириной ~6 блоков, но это оказались decorative-выступы боковых панелей,
не реальный игровой хитбокс. Пользователь подтвердил реальные габариты —
3x3 (ширина/глубина) x 4 (высота) блока. `VoxelShape` в
`FormixControlStationBlock` — `Shapes.box(-1,0,-1, 2,4,2)` — общий
bounding box под это, не точная форма.

**Определение "клика по нижней панели" — приближение, не точная
геометрия.** `BlockHitResult` даёт точку на грани ОДНОГО блока 1x1x1
(блока размещения), не произвольную 3D-точку внутри визуально большей
GeckoLib-модели. Разобрал geo.json-координаты вручную: самый верхний куб
группы "основание" (нижняя плоская площадка, "серый квадрат" с макета)
заканчивается на Y=12.7/16≈0.79 блока, следующая геометрия начинается на
Y≈0.625/1.06 блока. Порог `relativeY<=0.85` подобран с запасом между этими
значениями — компромисс, не точное решение.

**Конвертация `.bbmodel` → `.geo.json`/`.animation.json` — самописный
скрипт, не официальный Blockbench-экспортёр** (нет доступа к самому
Blockbench GUI). Проверено программно: 38/38 кубов активной модели и
28/28 неактивной корректно попали в дерево костей, анимация `idle` (loop,
6 сек) содержит реальные keyframes на 3 бонах. Текстуры (PNG 256x256,
реальный арт пользователя) извлечены из base64 внутри `.bbmodel`.

**НЕ проверено и требует внимания при первой сборке:**
1. GeckoLib maven-зависимость — пользователь добавляет сам, не проверено
   компилятором.
2. Корректность конвертации координат (mirror X между Blockbench и
   geo.json конвенциями) визуально в игре — риск зеркальности/сдвига,
   т.к. конвертер не проходил официальную Blockbench-валидацию, только
   программную проверку целостности (число кубов, наличие keyframes).
3. `relativeY<=0.85` эвристика клика — может потребовать точной
   подстройки после визуальной проверки в игре.

**Не реализовано в этой сессии (сознательно, по прямому ответу
пользователя):** сам HUD-экран терминала (Познание/Мир/Я/Разблокировать)
— заготовлен как `TODO` в `FormixControlStationBlock.use()`. Логика
переключения active/inactive состояния (`setActive()` в BlockEntity)
существует, но ничем не вызывается — ждёт будущей интеграции с системой
Эйдоса (`playerprogression/`, раздел 11 архива).

**ИСТОРИЧЕСКОЕ ПРИМЕЧАНИЕ (актуально на момент написания этого
подраздела, но УСТАРЕЛО по состоянию на конец 26.07.2026 — см. раздел
1.14 ниже):** архитектура "один блок + `active`/`isActive()` в
BlockEntity" была ПОЛНОСТЬЮ ОТМЕНЕНА в той же сессии, позже в тот же
день — пользователь явно попросил два раздельных блока/предмета вместо
одного с состоянием. Если этот подраздел (1.13) читается отдельно от
1.14 — не бери архитектуру `setActive()` за текущую реальность.

### 1.14 Терминал control station: переход на 2 блока + исправление build.gradle + missing texture — 26.07.2026, продолжение той же сессии

Три последовательные итерации после первой версии из раздела 1.13, все в
рамках одной непрерывной сессии с пользователем.

**Итерация 1 — build.gradle, синтаксическая ошибка `GeckoLib()`.**
Пользователь прислал скриншот ошибки `Could not find method GeckoLib()`.
Причина: `maven { name = 'GeckoLib' url '...' }` — Groovy разбирает
`name = '...'` как присваивание, а следующее `url '...'` без `=` — как
вызов метода на РЕЗУЛЬТАТЕ предыдущего выражения, отсюда попытка вызвать
несуществующий метод `GeckoLib()`. Моя ошибка — смешал официальный
GeckoLib-wiki синтаксис (`url '...'` без `=`) со стилем, уже
используемым в остальных repositories пользователя (`url = '...'`), не
подумав, что смешивание в одном блоке ломает парсинг.

**Итерация 2 — build.gradle, `fg` в неправильном блоке.** После
исправления №1 вылезла новая ошибка: `Could not get unknown property
'fg'`. Причина — я поместил `implementation fg.deobf(...)` внутрь
`buildscript { dependencies { ... } }`, где `fg` не существует (это
alias ForgeGradle-extension, доступный только ПОСЛЕ `apply plugin:
'net.minecraftforge.gradle'`, которая идёт позже в файле). Правильное
место — основной `dependencies { }` блок ближе к концу файла.

**Итерация 3 — build.gradle, версия GeckoLib не резолвится.** После
исправления №2 — `ModuleVersionNotFoundException` на
`software.bernie.geckolib:geckolib-forge-1.20.1:4.4.4`. Дал версию
`4.4.8` как альтернативу (подтверждённую на mvnrepository.com) — не
проверено, пользователь пока не пробовал. Параллельно дал fallback-путь
через `curse.maven:geckolib-388172:5675221` (версия 4.4.9, file ID с
CurseForge) — пользователь применил именно этот вариант и он сработал.
**Итог:** GeckoLib в этом проекте подключается через Curse Maven, не
напрямую с Cloudsmith — Cloudsmith maven-репозиторий в итоге вообще убран
из `buildscript.repositories` (не нужен, раз зависимость идёт через уже
существующий `cursemaven.com`).

**Итерация 4 — сборка прошла, но в игре missing texture (чёрно-
фиолетовый шахматный узор).** Пользователь прислал скриншот из игры:
блок стоит, но рендерится как ваниль missing-model заглушка. Причина
найдена через сравнение с рабочим `GarrisonEntryBlock` в проекте: у него
ЕСТЬ `blockstates/garrison_entry_block.json` +
`models/block/garrison_entry_block.json` (простой `parent: block/cross`
как placeholder-геометрия), которых я НЕ создал для терминала в первой
версии. `RenderShape.ENTITYBLOCK_ANIMATED` не освобождает от этого
требования — Minecraft всё равно проверяет наличие baked model в реестре
до того, как GeckoLib получает шанс отрендерить что-то через
`GeoBlockRenderer`. **Урок:** при добавлении GeckoLib-блока в этот
проект — ОБЯЗАТЕЛЬНО создавать тот же набор JSON, что и для ваниль-блоков
(`blockstates/`, `models/block/`, `models/item/`), даже если реальная
геометрия целиком идёт через отдельный рендерер. Не предполагать, что
`ENTITYBLOCK_ANIMATED` сам по себе достаточен.

**Одновременно — архитектурное решение пользователя: 2 блока вместо
1.** В том же обмене пользователь явно попросил "предмет активный и
неактивный" (во множественном числе) для креативного инвентаря. На
прямой уточняющий вопрос ("один блок с состоянием vs два раздельных
блока") пользователь выбрал ВТОРОЕ — два независимых
`BlockEntityType`/класса блока/GeoModel/рендерера. Вся архитектура
`active`/`setActive()`/`isActive()` из раздела 1.13 полностью удалена и
заменена: `FormixControlStationBlock`+`FormixControlStationBlockEntity`
(активный, всегда idle-анимация) и
`FormixControlStationInactiveBlock`+`FormixControlStationInactiveBlockEntity`
(неактивный, без анимаций — пустой `registerControllers()`). **Если в
проекте пользователя всё ещё лежит версия файлов из раздела 1.13 —
их нужно ПОЛНОСТЬЮ ЗАМЕНИТЬ файлами этой итерации, не патчить поверх**
(старые методы `setActive()`/`isActive()` удалены, поле `active` в NBT
`saveAdditional()`/`load()` тоже убрано).

**Файлы, изменённые/добавленные в этой итерации:**
- `blocks/FormixControlStationBlock.java` — переписан (убран флаг active)
- `blocks/FormixControlStationInactiveBlock.java` — новый
- `blocks/FormixControlStationBlockEntity.java` — переписан (без NBT
  active, всегда idle-анимация)
- `blocks/FormixControlStationInactiveBlockEntity.java` — новый (пустой
  `registerControllers()`)
- `blocks/FormixControlStationModel.java` — переписан (фиксированная
  модель/текстура, без переключения по `isActive()`)
- `blocks/FormixControlStationInactiveModel.java` — новый
- `blocks/FormixControlStationBlockRenderer.java` — без изменений в
  логике (конструктор теперь ссылается на упрощённый `GeoModel`)
- `blocks/FormixControlStationInactiveBlockRenderer.java` — новый
- `registrars/BlockRegistrar.java` — `FORMIX_CONTROL_STATION_BLOCK` +
  новый `FORMIX_CONTROL_STATION_INACTIVE_BLOCK`
- `registrars/BlockEntityRegistrar.java` — два раздельных
  `BlockEntityType` вместо одного
- `ClientModEvents.java` — два `registerBlockEntityRenderer` вызова
- НОВОЕ: `assets/reignofnether/blockstates/formix_control_station_block.json`,
  `..._inactive_block.json`
- НОВОЕ: `assets/reignofnether/models/block/formix_control_station_block.json`,
  `..._inactive_block.json` (оба `parent: block/cross`, ссылаются на
  соответствующую PNG-текстуру для fallback-вида в реестре)
- НОВОЕ: `assets/reignofnether/models/item/formix_control_station_block.json`,
  `..._inactive_block.json` (наследуют `models/block/...`, для превью
  предмета в инвентаре/руке — оба блока автоматически получили `BlockItem`
  через уже существующий 3-аргументный `registerBlock(name, block, tab)`,
  вручную `Item` не создавался)

**НЕ проверено в этой итерации (нет живого клиента):** сам факт, что
после добавления blockstate/model JSON текстура/модель отрендерится
корректно в игре (только логическая причина missing-texture исправлена,
визуальное подтверждение ждёт следующего теста пользователя); финальная
успешность компиляции Java против GeckoLib API через Curse Maven
зависимость (сборка build.gradle прошла, но `./gradlew build` с новым
Java-кодом ещё не подтверждён).

### 1.15 Item терминала в руке: полноценная 3D GeckoLib-модель + добавление в креатив-инвентарь — 26.07.2026, ночь

Пользователь прислал скриншот из игры: гигантская искажённая серая модель
в правом нижнем углу экрана. На уточняющий вопрос подтвердил — это
предмет терминала В РУКЕ (не блок, поставленный в мире; блок в мире ещё
не тестировался). Разбор показал: это НЕ баг координатной конвертации
`.bbmodel`→`geo.json` (geo.json уже был проверен ранее на целостность,
38/38 и 28/28 кубов) — это ожидаемое поведение ваниль item-рендера через
`models/item/*.json` с `parent: "reignofnether:block/formix_control_station_block"`
→ `block/cross`. GeckoLib-геометрия применяется ТОЛЬКО к реально
размещённому блоку через `BlockEntityRenderer` — авто-созданный `BlockItem`
(из 3-аргументного `registerBlock(name, block, tab)`) рендерит предмет по
ваниль-правилам (`block/cross` placeholder), не через GeckoLib вообще. Тот
же паттерн уже используется в проекте для `GarrisonEntryBlock` — его
предмет в руке выглядит так же "не финально", просто это было незаметно
на маленьком/невыделяющемся объекте, а на терминале (модель 3×4×3 блока)
это стало визуально заметной проблемой.

**Пользователь на прямой вопрос выбрал:** полноценную 3D GeckoLib-модель
в руке (не простую 2D-иконку, как у большинства блоков проекта) — явно
отмечено как "нетипично для этого проекта".

**Реализация — по официальной GeckoLib4 wiki "Geckolib Items" (раздел для
1.19.3→1.20.6 Forge, IClientItemExtensions):**
- `blocks/FormixControlStationBlockItem.java` /
  `FormixControlStationInactiveBlockItem.java` — новые, `extends BlockItem
  implements GeoItem`. `initializeClient()` override с
  `IClientItemExtensions.getCustomRenderer()` — стандартный Forge-хук для
  кастомного item-рендера (задокументирован именно для "1.19.3->1.20.6"
  ветки wiki, актуально для проекта на Forge 1.20.1).
  `SingletonGeoAnimatable.registerSyncedAnimatable(this)` в конструкторе —
  требуется для server-side анимационного sync (задел на будущее, сейчас
  не используется активно, но wiki рекомендует включать всегда).
- `blocks/FormixControlStationItemModel.java` /
  `FormixControlStationInactiveItemModel.java` — новые, `GeoModel<Item>`
  (типизированы по Item-классу, НЕ переиспользуют
  `FormixControlStationModel`, который типизирован по `BlockEntity` —
  `GeoModel<T>` жёстко привязан к конкретному animatable-типу). Ссылаются
  на ТЕ ЖЕ geo/texture/animation ассеты, что и блочная версия — модель в
  руке и в мире визуально идентичны.
- `blocks/FormixControlStationItemRenderer.java` /
  `FormixControlStationInactiveItemRenderer.java` — новые,
  `GeoItemRenderer<T>`.
- `models/item/formix_control_station(_inactive)_block.json` —
  ПЕРЕПИСАНЫ: было `parent: "reignofnether:block/..._block"` (ваниль
  fallback из предыдущей итерации 1.14), стало `parent: "builtin/entity"`
  — это специальный маркер, который говорит Minecraft "не рендери сам,
  спроси у `BlockEntityWithoutLevelRenderer`" — обязательный элемент по
  официальной документации ("My item is completely invisible" в разделе
  Common Issues wiki явно называет отсутствие этой строки причиной).

**Регистрация Item пришлось переделать — авто-BlockItem конфликтовал бы.**
`registerBlock(name, block, tab)` (3-аргументная версия, использовалась
для терминала с самого начала) автоматически создаёт обычный `BlockItem`
с тем же именем — если бы я добавил ещё и `FormixControlStationBlockItem`
под тем же registry name, была бы дублирующая регистрация. Исправлено:
`BlockRegistrar.java` — терминал переведён на 2-аргументную `registerBlock
(name, block)` (без `tab`, без авто-Item — версия уже существовала в
файле для других случаев, не нужно было её создавать). `Item` регистрируется
вручную в `ItemRegistrar.java` как `FORMIX_CONTROL_STATION_BLOCK_ITEM`/
`_INACTIVE_BLOCK_ITEM`.

**Добавление в креативный инвентарь — важное упущение из предыдущих
итераций, исправлено сейчас.** До этой итерации предметы терминала
ВООБЩЕ НЕ появлялись в креативе — 3-аргументный `registerBlock` кладёт
свои авто-Item в `BlockRegistrar.blockItems` Map, откуда их забирает
`CommonModEvents.creativeTabSetup()` для `CreativeModeTabs.FUNCTIONAL_BLOCKS`.
Ручные `GeoItem`-предметы в эту Map не попадают (она заполняется только
внутри `registerBlockItem()`, которая для терминала больше не вызывается).
Добавлено: `CommonModEvents.java`, `creativeTabSetup()`, `FUNCTIONAL_BLOCKS`
ветка — явные `event.accept(ItemRegistrar.FORMIX_CONTROL_STATION_BLOCK_ITEM.get())`
вызовы, тем же паттерном, что уже используется для `SPAWN_EGGS` в том же
методе (см. ту же ветку `if` чуть ниже в файле).

**Файлы этой итерации:**
- НОВЫЕ: `blocks/FormixControlStationBlockItem.java`,
  `FormixControlStationInactiveBlockItem.java`,
  `FormixControlStationItemModel.java`,
  `FormixControlStationInactiveItemModel.java`,
  `FormixControlStationItemRenderer.java`,
  `FormixControlStationInactiveItemRenderer.java`
- ИЗМЕНЕНЫ: `registrars/BlockRegistrar.java` (2-аргументная регистрация
  для терминала), `registrars/ItemRegistrar.java` (ручная регистрация
  двух `GeoItem`), `CommonModEvents.java` (`creativeTabSetup` —
  `FUNCTIONAL_BLOCKS` ветка),
  `models/item/formix_control_station(_inactive)_block.json` (parent
  сменён на `builtin/entity`)
- НЕ изменены в этой итерации: `blockstates/*.json`, `models/block/*.json`
  (оба всё ещё нужны для самого БЛОКА в мире — реестр Minecraft всё ещё
  требует их наличия независимо от того, как рендерится ITEM)

**НЕ проверено (нет живого клиента):** сам факт, что
`IClientItemExtensions`/`GeoItemRenderer` в руке действительно покажет
модель без искажений — это первый GeckoLib-Item в проекте, паттерн
скопирован из официальной wiki максимально буквально, но не обкатан на
практике в этом конкретном codebase. Если после сборки предмет в руке
всё ещё выглядит некорректно (неправильный масштаб/поворот) — GeckoLib
обычно решает такие вещи через `display` секцию в item model json
(`transforms.thirdperson_righthand` и т.п.) или через переопределение
пивота модели в geo.json — не пробовано, т.к. нет данных о конкретной
ошибке без скриншота после сборки.

### 1.16 Полёт/noclip в Survival (две отдельные причины) + процедура правильного теста population-бага — 26.07.2026, ночь

Этот подраздел перенесён из раздела 0 при его компрессии 26.07.2026,
ночь — деталей не терялось, только перенесено из "снимка состояния" в
архив истории по датам. Автор изначальных правок в этой сессии — другая
сессия/чат Claude в этом же проекте (по стилю и содержанию комментариев
в коде, но точная сессия неизвестна текущему контексту).

**Часть A — полёт/noclip в Survival, две отдельные причины одного
внешнего симптома.**

Пользователь подтвердил на практике: в Survival можно летать и проходить
сквозь блоки сразу при заходе в мир, ещё до первого нажатия F12, и после
захода в orthoview и выхода из него всё становится нормально.

**Причина 1 (изначально предполагаемая, тоже реальна): не сбрасывался
полёт при ВЫХОДЕ из orthoview.** `PlayerMixin.tick()` выставлял
`noPhysics=true`/`flying=true` для non-Creative игроков, пока они в
orthoview, но никогда не сбрасывал их обратно при выходе — оригинальный
комментарий в коде ("tick() сам всё вернёт, отдельный сброс не нужен")
был неверным предположением автора мода. **Исправлено:** `PlayerMixin.
tick()` получил явную `else`-ветку — если сущность больше не в orthoview,
но `noPhysics` всё ещё `true` и игрок не в Creative — оба флага
сбрасываются. `PlayerServerEvents.disableOrthoview()` намеренно НЕ
дублирует эту логику (только убирает игрока из списка) — сброс
происходит централизованно в миксине на следующий тик, чтобы не повторить
уже дважды пойманный в этом проекте паттерн "одна и та же логика в двух
местах расходится при будущих правках".

**Причина 2 (настоящая причина конкретно "летаю сразу при заходе, ещё до
F12"): `OrthoviewClientEvents.enabled` — статическое поле, не привязанное
к жизненному циклу мира.** Единственное место, где `enabled` меняется —
`toggleEnable()` (`enabled = !enabled`). Если предыдущая игровая сессия
закончилась, пока игрок был в orthoview (Save & Quit, разрыв соединения,
краш — НЕ через явный повторный F12), поле `enabled` остаётся `true`
даже после выхода из того мира. При следующем заходе в мир (в рамках
того же запуска клиента, JVM не перезапускался) `PlayerMixin.tick()` с
первого тика видит `OrthoviewClientEvents.isEnabled() == true` и
немедленно форсирует `flying`/`noPhysics`, ещё до того как игрок хоть
раз нажал F12 в этой сессии. **Исправлено:** новый обработчик
`OrthoviewClientEvents.onClientLogin(ClientPlayerNetworkEvent.LoggingIn)`
— сбрасывает `enabled = false` и `enabledCount = 0` при каждом входе в
мир, независимо от того, как закончилась предыдущая сессия. Паттерн
(`@SubscribeEvent` на `ClientPlayerNetworkEvent.LoggingIn`) взят из уже
существующего в проекте `config/ConfigClientEvents.onClientLogin`.

**Почему "зайти в orthoview и выйти" чинит проблему в моменте:**
`toggleEnable()` — единственное место, которое реально меняет `enabled`.
Зайдя в orthoview и выйдя из него нормально (через F12 туда-обратно) в
текущей сессии, `enabled` корректно становится `false` на выходе — баг
временно исчезает до следующего "плохого" выхода из мира.

**Не проверено вживую компилятором.** Нужно протестировать 3 сценария
отдельно:
1. Зайти в Survival сразу (без предыдущего "плохого" выхода в orthoview)
   — раньше и это иногда работало нормально, проверить, что не сломалось.
2. Специально выйти из мира прямо находясь в orthoview (не через F12,
   через меню паузы/выход), зайти заново — раньше сразу летал бы, теперь
   не должен.
3. Обычный сценарий — зайти в orthoview (F12), выйти (F12) в течение
   одной сессии — пробел/шифт должны работать как обычно сразу после.

Файлы, изменённые: `mixin/PlayerMixin.java`,
`player/PlayerServerEvents.java`, `orthoview/OrthoviewClientEvents.java`.

**Часть B — почему первый `latest.log` для population-бага не содержал
диагностических строк, и точная процедура для следующего теста.**

Оба метода (`[FORMIX-DEBUG-CLIENT-SUPPLY]` и `[FORMIX-DEBUG-CLIENT]`)
вызываются ТОЛЬКО из `HudClientEvents.onDrawScreen()`, а этот метод в
самом начале имеет жёсткое условие выхода:
```java
if (!OrthoviewClientEvents.isEnabled() || !(evt.getScreen() instanceof TopdownGui) || !enabled) {
    return;
}
```
То есть строка population рисуется (и, значит, лог печатается) ТОЛЬКО
когда игрок УЖЕ находится в RTS top-down режиме (orthoview). В присланном
логе пользователь кликнул по `FormixHive` ДО входа в orthoview, затем
включил orthoview, но после этого не выбрал ни одного юнита/здания
заново — панель population не отрисовалась, лог не мог сработать.

**Точная процедура для следующего теста (передать пользователю
дословно):**
1. Построить/убедиться, что `FormixHive` уже построен.
2. Нажать F12 — войти в orthoview.
3. **Находясь уже в orthoview**, кликнуть по зданию `FormixHive` ИЛИ по
   любому своему юниту, чтобы выделить его.
4. Подержать выделение хотя бы 2-3 секунды (логи throttled, раз в
   секунду).
5. Выйти, прислать `run/logs/latest.log`, поискать оба префикса
   (`[FORMIX-DEBUG-CLIENT-SUPPLY]` и `[FORMIX-DEBUG-CLIENT]`).

**Урок, уже дважды подтверждённый в этом проекте:** методы с одинаковым
именем существуют раздельно в `Xxx**Server**Events` и `Xxx**Client**Events`
классах (см. раздел 1.12 архива). **Третий по счёту урок того же типа:**
прежде чем полагаться на throttled-диагностику внутри метода, вызываемого
условно (по конкретному экрану/режиму) — нужно явно проверить и сообщить
пользователю точную последовательность действий, гарантированно вызывающую
этот метод хотя бы раз, а не просто "какой лог искать".


### 1.17 Полёт/noclip в Survival — найдена третья, настоящая причина + вкладка Formix в творческом режиме — 26.07.2026, ночь (продолжение)

**Часть A — фикс из раздела 1.16 оказался неполным, найдена реальная
причина.** Пользователь протестировал первый фикс (сброс клиентского
`OrthoviewClientEvents.enabled` на логине, раздел 1.16 архива) — баг
остался: полёт/noclip в Survival всё ещё происходили сразу при заходе в
мир, ещё до первого нажатия F12.

**Настоящая причина — серверная, не клиентская.**
`PlayerServerEvents.onPlayerJoin()` содержал блок, который безусловно
добавлял КАЖДОГО заходящего игрока в `orthoviewPlayers`, если его id
(новый, переприсваиваемый при каждом входе в мир) ещё не встречался в
списке — что истинно для абсолютно любого входа, включая самый первый
вход новой игровой сессии. Сервер сам, автоматически, считал любого
зашедшего игрока "находящимся в orthoview". `PlayerMixin.tick()` видел
это через `orthoIds.contains(id)` и форсировал `flying`/`noPhysics` с
первого тика — независимо от состояния клиентского `enabled`, поэтому
фикс из раздела 1.16 не решил проблему: сервер добавлял игрока в список
заново при каждом входе, перекрывая клиентское состояние.

Судя по всему, это была неудачная попытка автора мода "восстановить
состояние orthoview после переподключения", но условие написано
некорректно — оно не может отличить "этот же игрок вернулся, будучи в
orthoview" от "просто новый вход", потому что `Entity.getId()`
переприсваивается при каждом заходе в мир.

**Исправлено:** блок автодобавления в `onPlayerJoin()` удалён полностью
(не переписан — не существует надёжного способа восстановить "был ли
игрок в orthoview" по одному только новому id). Список `orthoviewPlayers`
теперь пополняется ИСКЛЮЧИТЕЛЬНО через явный `enableOrthoview(id)` (F12).
Дополнительно `onPlayerLeave()` теперь тоже чистит `orthoviewPlayers` по
id уходящего игрока (раньше чистились только `players`, `orthoviewPlayers`
копился без чистки при выходе — источник утечки памяти на устаревшие
`ServerPlayer`-объекты, отдельно от основного бага, но тоже мог влиять на
долгих игровых сессиях с множеством входов/выходов).

**Итоговый список всех трёх причин одного и того же внешнего симптома
("летаю/прохожу сквозь блоки в Survival"):**
1. (Раздел 1.16) Не сбрасывался полёт при ВЫХОДЕ из orthoview в течение
   сессии — `PlayerMixin.tick()` не имел `else`-ветки для сброса.
2. (Раздел 1.16) Клиентское `OrthoviewClientEvents.enabled` не
   сбрасывалось на новый вход в мир после "плохого" выхода из предыдущей
   сессии (Save & Quit прямо в orthoview).
3. (Этот подраздел, настоящая причина отчёта пользователя) Серверный
   `PlayerServerEvents.onPlayerJoin()` безусловно добавлял любого
   зашедшего игрока в `orthoviewPlayers`.

**Урок на будущее, важный для этого класса багов:** когда причина в
"состоянии, которое переживает мир/сессию, но не должно" — это почти
никогда одно место. В этом проекте уже есть клиент/сервер разделение
почти для каждой подсистемы (см. `ARCHITECTURE.md`) — если баг похож на
"что-то не сбрасывается", нужно грепать `grep -rn "переменная\s*="` и
проверять КАЖДОЕ место присвоения на ОБЕИХ сторонах (клиент И сервер), а
не останавливаться после первого правдоподобного найденного места и не
считать один фикс достаточным без явного подтверждения от пользователя,
что баг реально пропал после теста.

**Файлы, изменённые за обе попытки исправления:** `mixin/PlayerMixin.java`,
`player/PlayerServerEvents.java` (дважды — `disableOrthoview()` в первую
итерацию, `onPlayerJoin()`/`onPlayerLeave()` во вторую),
`orthoview/OrthoviewClientEvents.java`.

**Не проверено вживую после третьего фикса** — нужно протестировать
заново: (1) обычный вход в Survival, (2) выход из мира прямо в orthoview
и повторный вход, (3) F12 туда-обратно в течение сессии. Если проблема
всё ещё останется — следующий шаг: временный лог в `PlayerMixin.tick()`,
печатающий `orthoIds.contains(id)` и `OrthoviewClientEvents.isEnabled()`
раздельно при первом тике после спавна.

**Часть B — отдельная вкладка "Formix" в творческом режиме.**

Пользователь попросил удобный способ брать Formix-предметы в креативе (до
этого терминал был спрятан внутри ваниль-вкладки `FUNCTIONAL_BLOCKS`
вперемешку с сотнями других блоков) и явно подтвердил, что вкладка
должна включать ВСЕ текущие и будущие Formix-предметы/блоки, не только
терминал.

**Реализация:**
- Новый `registrars/CreativeModeTabRegistrar.java` —
  `DeferredRegister<CreativeModeTab>` (официальный Forge-паттерн для
  1.20.1, `Registries.CREATIVE_MODE_TAB`), единственная вкладка
  `FORMIX_TAB`. Иконка вкладки — `ItemStack` активного предмета
  терминала (`ItemRegistrar.FORMIX_CONTROL_STATION_BLOCK_ITEM`).
- **Централизованный список `FORMIX_TAB_ITEMS`** (статический блок
  `List<Supplier<? extends RegistryObject<? extends Item>>>`) — единая
  точка добавления новых Formix-предметов в будущем. Чтобы добавить
  предмет в вкладку, достаточно одной строки в этом списке — не нужно
  трогать `ItemRegistrar.java`/`CommonModEvents.java` вручную каждый раз,
  как было нужно для самого терминала (см. раздел 1.15 архива, там
  потребовалась ручная разводка через `CommonModEvents.creativeTabSetup()`
  именно потому, что GeoItem не проходит через авто-`BlockItem`-путь).
  Список читается один раз в `displayItems()` и работает независимо от
  того, как конкретный `Item` был зарегистрирован (авто-BlockItem, ручной
  GeoItem, обычный Item, спавн-яйцо) — снимает необходимость помнить,
  какой путь регистрации использовался у конкретного предмета.
- `CommonModEvents.java` — терминал убран из ветки `FUNCTIONAL_BLOCKS` в
  `creativeTabSetup()` (раньше дублировался там и в новой вкладке —
  теперь только в `FORMIX_TAB`).
- `ReignOfNether.java` — `CreativeModeTabRegistrar.init(mlctx)` добавлен
  в конструктор мода, после `BlockEntityRegistrar.init()` (важно ПОСЛЕ
  `ItemRegistrar`/`BlockRegistrar`, т.к. иконка вкладки ссылается на уже
  зарегистрированный `RegistryObject<Item>`).
- `lang/en_us.json` — новый ключ `itemGroup.reignofnether.formix` = "Formix"
  (название вкладки в интерфейсе), добавлен через Python json-парсинг, не
  текстовым поиском-заменой (тот же метод, что уже использовался для
  переименования ресурсов, раздел 12.1 архива).
- `PROJECT_NOTES/QUICK_REFERENCE.md` — новый раздел 4.5 "Рецепт: добавить/
  заменить GeckoLib-модель блока" — общая процедура на будущее (что мне
  нужно от пользователя при отправке нового `.bbmodel` для блока, что я
  сделаю на своей стороне, известные грабли типа обязательных blockstate/
  model JSON). Раздел 2 (структура проекта) дополнен упоминанием
  `registrars/CreativeModeTabRegistrar.java` и путей GeckoLib-ассетов
  (`geo/`, `animations/`, `textures/block/`).

**НЕ проверено вживую:** сама вкладка (иконка, порядок предметов,
название) — паттерн взят из официальной Forge-документации максимально
буквально, но не обкатан в этом конкретном codebase (первая кастомная
`CreativeModeTab` в проекте — раньше использовались только ваниль-вкладки).


## 2. Концепция фракции

- Фракция называется **Formix**.
- Основа — **полностью кастомные мобы** (не ре-текстур ваниль-моба), модели
  делаются в Blockbench пользователем.
- **Worker/Warrior механика** как у Villagers/Militia:
    - `FormixWorkerUnit` — рабочий: собирает ресурсы, строит/чинит здания.
    - `FormixWarriorUnit` — боевая форма, получается конверсией из worker'а
      у главного здания (`FormixHive`), возврат обратно возможен через
      ability "Back to Work".
- Один тип production-здания на этом этапе: `FormixHive` (аналог TownCentre),
  производит только `FormixWorkerUnit`. Warrior производится ТОЛЬКО через
  конверсию, отдельного Prod-класса для него нет и не нужно.

## 3. Реестр новых/изменённых файлов

### 3.1 Новые файлы (полностью написаны с нуля)

| Файл | Назначение |
|---|---|
| `unit/units/formix/FormixWorkerUnit.java` | Рабочий юнит. `extends Monster`, `implements Unit, WorkerUnit, AttackerUnit, KeyframeAnimated, ConvertableUnit` |
| `unit/units/formix/FormixWarriorUnit.java` | Боевой юнит. `extends Monster`, `implements Unit, AttackerUnit, KeyframeAnimated, ConvertableUnit` |
| `unit/units/formix/FormixWorkerProd.java` | Production item — производство worker'а в здании |
| `unit/goals/CallToArmsGoalFormix.java` | AI-goal: движение worker'а к FormixHive + конверсия в warrior. 1:1 копия `CallToArmsGoal` (Villagers) с заменой типов |
| `ability/abilities/formix/CallToArmsUnitFormix.java` | Ability юнита: "иди к Hive и стань воином" |
| `ability/abilities/formix/CallToArmsBuildingFormix.java` | Ability здания: призвать всех worker'ов в радиусе |
| `ability/abilities/formix/BackToWorkUnitFormix.java` | Ability юнита: warrior → worker |
| `ability/abilities/formix/BackToWorkBuildingFormix.java` | Ability здания: вернуть всех warrior'ов в радиусе к работе |
| `building/buildings/formix/FormixHive.java` | Главное здание фракции. **ВРЕМЕННО** использует NBT-структуру `town_centre` (см. раздел 4) |
| `unit/modelling/models/FormixWorkerModel.java` | PLACEHOLDER-модель (простая гуманоид-геометрия из кубов). Заменить на Blockbench-экспорт |
| `unit/modelling/models/FormixWarriorModel.java` | PLACEHOLDER-модель (чуть шире плечи, чтобы отличаться визуально) |
| `unit/modelling/renderers/FormixWorkerRenderer.java` | Рендерер worker'а, ссылается на текстуру `textures/entities/formix_worker_unit.png` |
| `unit/modelling/renderers/FormixWarriorRenderer.java` | Рендерер warrior'а, текстура `formix_warrior_unit.png` |
| `unit/modelling/animations/FormixAnimations.java` | PLACEHOLDER keyframe-анимации (idle/walk/attack), минимальные |
| `assets/reignofnether/textures/entities/formix_worker_unit.png` | Программно сгенерированная плоско-цветная 64×64 заглушка (амбер) |
| `assets/reignofnether/textures/entities/formix_warrior_unit.png` | Заглушка 64×64 (тёмно-красный) |
| `assets/reignofnether/textures/mobheads/formix_worker.png` | Иконка 16×16 для UI (та же амбер-заливка) |
| `blocks/FormixControlStationBlock.java` | Блок-терминал (26.07.2026) — `EntityBlock`, обработка клика по нижней панели → RTS-режим |
| `blocks/FormixControlStationBlockEntity.java` | `GeoBlockEntity`, хранит active/inactive состояние терминала |
| `blocks/FormixControlStationModel.java` | `GeoModel`, переключает geo/texture по active/inactive |
| `blocks/FormixControlStationBlockRenderer.java` | `GeoBlockRenderer` (активный) |
| `blocks/FormixControlStationInactiveBlock.java` | Неактивный блок терминала (26.07.2026, поздний вечер) — отдельный класс, не флаг у активного |
| `blocks/FormixControlStationInactiveBlockEntity.java` | `GeoBlockEntity` неактивного, без анимаций |
| `blocks/FormixControlStationInactiveModel.java` | `GeoModel` неактивного |
| `blocks/FormixControlStationInactiveBlockRenderer.java` | `GeoBlockRenderer` неактивного |
| `assets/reignofnether/blockstates/formix_control_station(_inactive)_block.json` | Обязателен даже с `ENTITYBLOCK_ANIMATED` — иначе missing texture |
| `assets/reignofnether/models/block/formix_control_station(_inactive)_block.json` | `parent: block/cross`, placeholder-геометрия для реестра |
| `assets/reignofnether/models/item/formix_control_station(_inactive)_block.json` | `parent: builtin/entity` — полноценная 3D GeckoLib-модель в руке/инвентаре, НЕ авто-BlockItem |
| `blocks/FormixControlStationBlockItem.java`, `..._InactiveBlockItem.java` | `BlockItem implements GeoItem` — ручная регистрация, показывает 3D-модель в руке |
| `blocks/FormixControlStationItemModel.java`, `..._InactiveItemModel.java` | `GeoModel<Item>` для рендера в руке (те же geo/texture/animation, что у блока) |
| `blocks/FormixControlStationItemRenderer.java`, `..._InactiveItemRenderer.java` | `GeoItemRenderer` |
| `registrars/CreativeModeTabRegistrar.java` | Вкладка "Formix" в творческом режиме (26.07.2026, ночь) — централизованный список `FORMIX_TAB_ITEMS`, добавить новый предмет = 1 строка |
| `assets/reignofnether/geo/formix_control_station(_diactive).geo.json` | GeckoLib-геометрия (сконвертирована из `.bbmodel` пользователя) |
| `assets/reignofnether/animations/formix_control_station.animation.json` | GeckoLib-анимация idle |
| `assets/reignofnether/textures/block/formix_control_station(_diactive).png` | Текстуры терминала (реальный арт пользователя, 256×256) |

### 3.2 Изменённые существующие файлы (расширены, НЕ переписаны)

| Файл | Что добавлено |
|---|---|
| `faction/Faction.java` | `FORMIX` в enum |
| `faction/FactionRegistries.java` | `FactionRegister FORMIX`, регистрация `Buildings.FORMIX_HIVE` |
| `player/PlayerAction.java` | `START_RTS_FORMIX` |
| `player/PlayerServerboundPacket.java` | Client: выбор фракции Formix → `START_RTS_FORMIX` (в т.ч. в RANDOM-пуле). Server: обработка `START_RTS_FORMIX` → `startRTS(..., Faction.FORMIX)` |
| `player/PlayerServerEvents.java` | 3 switch: спавн стартового worker'а (игрок), спавн стартового здания (игрок), спавн worker'а (бот) — все получили `case FORMIX`/`case PIGLINS -> FORMIX_HIVE` |
| `util/MiscUtil.java` | Иконка фракции в `getFactionIcon`-подобном switch |
| `hud/buttons/HelperButtons.java` | Иконка idle-worker кнопки |
| `sandbox/SandboxClientEvents.java` | Цикл переключения фракции (◀▶), список названий фракций, building buttons switch, unit spawn buttons switch — все получили Formix |
| `survival/SurvivalClientEvents.java` | Название фракции в тултипе волны |
| `registrars/EntityRegistrar.java` | `FORMIX_WORKER_UNIT`, `FORMIX_WARRIOR_UNIT` EntityType регистрация + resolver switch (`FormixWorkerProd.itemName`) |
| `CommonModEvents.java` | Атрибуты (`createAttributes()`) для обоих юнитов |
| `ClientModEvents.java` | Регистрация рендереров + layer definitions для обеих моделей |
| `resources/ResourceCosts.java` | `FORMIX_WORKER`, `FORMIX_WARRIOR`, `FORMIX_HIVE` ResourceCost + bakeValues() |
| `config/ReignOfNetherCommonConfigs.java` | Config entries (`UnitCosts.FORMIX_WORKER/WARRIOR`, `BuildingCosts.FORMIX_HIVE`) + `.define(BUILDER)` вызовы |
| `building/production/ProductionItems.java` | Регистрация `FORMIX_WORKER` (FormixWorkerProd) |
| `building/Buildings.java` | Регистрация `FORMIX_HIVE` |
| `assets/reignofnether/lang/en_us.json` | Все строки: hud.faction, buildings.formix_hive, entity.formix_worker/warrior_unit, abilities.*_formix (4 ability + tooltips) |

### 3.3 Файлы, которые СОЗНАТЕЛЬНО не тронуты (см. обоснование)

- `survival/Wave.java` — AI-волны против игрока пока не включают Formix
  (default case сработает нормально). Добавить позже, если понадобятся
  survival-волны атакующих Formix-мобов.
- `scenario/ScenarioServerboundPacket.java` — сценарии/кампании не расширены
  под Formix. Не входило в minimal pipeline.
- `sounds/SoundClientEvents.java` — музыкальная тема фракции не добавлена
  (нет аудио-ассета).
- `unit/units/villagers/*`, `ability/abilities/CallToArms*.java` (не-formix
  версии) — НЕ изменены, чтобы не трогать рабочий код Villagers. Formix
  использует свои параллельные классы, переиспользуя только те же
  `UnitAction` enum-константы (это безопасно — action используется только
  как ID кнопки/сети, не диспетчеризует логику централизованно).

## 4. Временные решения (TODO при появлении твоих ассетов)

### 4.1 Модель/анимации (приоритет — ты сказал, что уже делаешь в Blockbench)

Когда будет готов реальный экспорт из Blockbench для worker и warrior:

1. Открой экспортированный `.java` класс модели (Blockbench → File → Export →
   Export Java Entity). Скопируй его содержимое.
2. В `FormixWorkerModel.java` / `FormixWarriorModel.java` замени:
    - Поле `LAYER_LOCATION` — оставь как есть (или возьми имя из экспорта,
      не важно, лишь бы совпадало с `ClientModEvents` регистрацией).
    - Тело `createBodyLayer()` — вставь из экспорта.
    - Список полей `ModelPart` (сейчас `body/head/left_arm/right_arm/left_leg/right_leg`)
      — подстрой под реальные названия частей твоей модели, а также
      обнови `setupAnim()`, если имена частей отличаются.
    - Класс `KeyframeHierarchicalModel<T>` как родитель можно оставить, если
      будешь использовать keyframe-анимации (рекомендуется, как у Wraith).
      Если Blockbench-экспорт даёт другой базовый класс — подстройся под него,
      смотри как это сделано в `unit/modelling/models/WraithModel.java` для
      референса реального экспорта.
3. Замени `unit/modelling/animations/FormixAnimations.java` на экспорт
   анимаций из Blockbench (File → Export → Export Animations). Оставь
   имя класса `FormixAnimations` и статические поля `IDLE`/`WALK`/`ATTACK`
   (или добавь новые и обнови ссылки в `FormixWorkerUnit`/`FormixWarriorUnit`
   / `*Model.java`, где они вызываются).
4. Замени PNG-текстуры:
    - `assets/reignofnether/textures/entities/formix_worker_unit.png`
    - `assets/reignofnether/textures/entities/formix_warrior_unit.png`
      Просто перезапиши файл тем же именем — рендереры уже на него ссылаются,
      ничего в Java менять не надо.
5. Если хочешь визуально более уникальную иконку в UI (сейчас общий
   worker.png плоского цвета) — обнови также
   `textures/mobheads/formix_worker.png`.

### 4.2 Здание FormixHive — своя NBT-структура

Сейчас `FormixHive.structureName = "town_centre"` — использует чужую
NBT-схематику Villagers, чтобы всё компилировалось и было играбельно.

Когда будет готова своя структура:
1. Построй здание вручную в игре (creative), обозначь углы структуры
   структурным блоком (structure block), сохрани через `/structure save
   reignofnether:formix_hive` (или как принято в этом проекте — посмотри,
   как сделаны другие `.nbt` в `resources/structures/`, вероятно есть
   собственный workflow/скрипт у автора мода — проверь `TODO.txt`/
   `TODO_backlog.txt` в корне репо, там могут быть заметки автора).
2. Сохрани файл как:
    - `src/main/resources/assets/reignofnether/structures/formix_hive.nbt`
    - `src/main/resources/data/reignofnether/structures/formix_hive.nbt`
      (оба пути — так сделано для `hoglin_stables.nbt`, см. пример).
3. В `FormixHive.java` смени `structureName = "town_centre"` на
   `structureName = "formix_hive"`.
4. Обнови `portraitBlock`/`icon` на подходящий блок вместо `Blocks.WARPED_NYLIUM`.

### 4.3 Локализация — только английский

`en_us.json` заполнен. Другие языки (проект использует Crowdin, см.
`crowdin.yml` в корне) не тронуты — это ок, English — язык по умолчанию,
остальные подтянутся через обычный процесс перевода проекта.

## 5. Архитектурные решения, которые стоит помнить

- **UnitAction переиспользование**: Formix-абилки используют те же
  `UnitAction.CALL_TO_ARMS_UNIT/BUILDING`, `BACK_TO_WORK_UNIT/BUILDING`
  enum-константы, что и Villagers. Это безопасно, т.к. `UnitAction` в этом
  проекте — просто ID для кнопки/сетевого пакета, диспетчеризация происходит
  через сам объект `Ability`, прикреплённый к конкретному юниту/зданию
  (`this.abilities.add(...)`), а не через центральный switch по `UnitAction`.
  Если это когда-то изменится (кто-то добавит централизованный switch по
  UnitAction) — тогда потребуется развести на отдельные значения.
- **I18n-ключи РАЗВЕДЕНЫ**: в отличие от UnitAction, для текста в `en_us.json`
  Formix-абилки используют СВОИ ключи (с суффиксом `_formix`), а не общие
  `abilities.reignofnether.call_to_arms_unit` и т.д. Причина: те ключи
  жёстко содержат текст про "Militia"/"Town Centre", который был бы неверен
  для Formix.
- **FormixWarriorUnit не implements WorkerUnit** — сознательно, т.к. warrior
  не занимается сбором ресурсов/стройкой, только боем. Хранит
  `resourcesSaveData` (тип `TargetResourcesSave`) чтобы восстановить
  worker'у его прежнее назначение при обратной конверсии.
- **Базовый класс — `Monster`, не наследование ваниль-моба.** В отличие от
  Piglins-юнитов (`GruntUnit extends Piglin` и т.п.), Formix-юниты следуют
  паттерну `WraithUnit extends Monster` — т.к. используют полностью
  кастомную (не ваниль) модель. Это правильный путь для кастомных мобов.
- **WARRIOR_RANGE = 60** (константа в `FormixHive`) — дистанция, на которой
  warrior остаётся конвертированным, аналог `TownCentre.MILITIA_RANGE`.
  Можно потом вынести в конфиг, если понадобится балансировка.
- **Стоимости (config defaults)**:
    - `FormixWorker`: 50 food, 15 сек, 1 pop (= Grunt/Villager baseline)
    - `FormixWarrior`: 0 food/food-cost, но "стоит" 20 wood (символическая
      стоимость конверсии, т.к. Prod-класса нет — это просто дефолтное
      значение конфига, реально не расходуется при конверсии сейчас).
      **Пересмотри это значение**, возможно стоит сделать конверсию
      полностью бесплатной (0 во всех полях) либо явно списывать ресурсы в
      `convertToWarrior()`, если хочешь баланс "конверсия стоит ресурсов".
    - `FormixHive`: 400 wood, 0 ore (= похоже на TownCentre, но без ore-cost
      — подправь под баланс своей игры).

## 6. Известные ограничения текущего MVP (сознательно упрощено)

- `FormixWarriorUnit` — **только melee**, нет bow/ranged формы (в отличие
  от `MilitiaUnit`, который умеет swapWeapons между sword/bow). Если
  понадобится дальнобойная форма — придётся добавить аналог `swapWeapons()`
  и второй набор анимаций/goal (`UnitBowAttackGoal`).
- Нет "captain"/banner/veteran exp механики (у MilitiaUnit есть — banner
  делает юнита неуязвимым к авто-конверсии обратно, veteran exp система
  профессий). Formix её не имеет — сознательное упрощение для MVP.
- Нет garrison-specific текстур/логики сверх базового `GarrisonGoal` (сам
  `GarrisonGoal` подключен и должен работать "из коробки", т.к. это общий
  класс не завязанный на фракцию).
- `getBuildingButtons()` в `FormixWorkerUnit` не поддерживает custom
  buildings (`CustomBuildingClientEvents.customBuildings`) — оставлен
  пустой TODO-цикл. Villagers/другие фракции такое умеют, стоит доразобрать
  как это устроено у них и повторить, если нужны user-created buildings
  для Formix.
- Только один production building (`FormixHive`). Нет второго/третьего
  здания фракции (казармы, ресурсное здание и т.п.) — по договорённости,
  это следующий этап после обкатки этого MVP.

## 7. Как продолжать в следующей сессии

0. Если работаешь через Claude Projects — этот файл уже должен быть в
   Knowledge проекта, а `CLAUDE_PROJECT_INSTRUCTIONS.md` задаёт поведение
   автоматически на каждый чат. Если ещё не настроено — см. раздел
   "Если хочешь работать через Claude Projects" в `README_FIRST.md`.
1. Прочитай этот файл целиком перед тем как начинать.
2. Собери проект (`./gradlew build`), пришли мне ошибки компиляции если
   будут — почти наверняка это будут мелкие вещи (незамеченный private
   метод, лишний/недостающий импорт).
3. Если у тебя готовы Blockbench-модели — следуй разделу 4.1.
4. Если хочешь второе здание/юнита — опиши роль (что производит, что
   защищает и т.п.), я применю тот же паттерн, что здесь.
5. Не переписывай `unit/units/villagers/*` или `ability/abilities/*` (не-
   formix версии) "заодно" — они рабочие, трогать их не нужно.

## 8. Быстрый чеклист "добавить ещё одного боевого юнита Formix" (шаблон на будущее)

Раз этот MVP уже задаёт паттерн, следующий юнит — это по сути повторение
шагов 1-и-2 без необходимости трогать фракционную инфраструктуру:

1. Модель + рендерер + анимации (по образцу FormixWorkerModel/Renderer).
2. Класс юнита `extends Monster implements Unit, AttackerUnit,
   KeyframeAnimated` (без WorkerUnit/ConvertableUnit, если это не
   worker-конвертируемый юнит) — копируй структуру `FormixWarriorUnit.java`.
3. `XxxProd.java` — копируй `FormixWorkerProd.java`, поменяй itemName/cost/
   EntityRegistrar-ссылку.
4. `EntityRegistrar` — EntityType регистрация.
5. `CommonModEvents` — атрибуты.
6. `ClientModEvents` — рендерер + layer.
7. `ResourceCosts` + `ReignOfNetherCommonConfigs` — cost + define().
8. `ProductionItems` — регистрация Prod.
9. `FormixHive.java` — добавить `this.productions.add(ProductionItems.XXX,
   Keybindings.abilitySlotN)` в конструкторе.
10. `en_us.json` — `entity.reignofnether.xxx_unit` + tooltip строки.
11. `sandbox/SandboxClientEvents.java` — добавить кнопку в `case FORMIX ->`
    блок `getUnitButtons()`.

## 10. Про Claude Project и GitHub-коннектор — 21.07.2026

Пользователь показал скриншот настроек Claude (Settings → Connectors), где
"GitHub Integration" помечен как **connected** на уровне аккаунта. Важное
уточнение, зафиксированное в этой сессии: подключение коннектора к
аккаунту НЕ означает автоматическую доступность в каждом чате — коннекторы
могут быть включены точечно per-чат/per-проект. В сессии, где велось это
обсуждение, GitHub-инструмент физически не подгрузился при `tool_search`
(только Notion-инструменты были доступны), несмотря на видимый статус
"connected" в общих настройках. Вероятная причина: коннектор активен для
ДРУГОГО чата/проекта пользователя ("Переработка GitHub проекта" был виден
в списке недавних чатов на скриншоте), не для этого разговора.

**Что это значит для будущих сессий этого проекта:** если пользователь
создаст Claude Project для этого мода и подключит туда GitHub — **не
предполагай автоматически, что GitHub-инструмент доступен**, даже если
пользователь упоминал, что "включил" его где-то. В начале сессии, где
GitHub может быть релевантен (например пользователь просит прочитать
файл из репозитория напрямую) — сначала попробовать `tool_search` с
релевантными терминами ("github", "repository", "pull request" и т.п.), и
если ничего не найдётся — прямо сказать пользователю, что инструмент не
подгрузился в этом чате, и спросить, включён ли он именно для этого
чата/проекта, а не просто "подключён к аккаунту".

**Даже если GitHub-инструмент когда-то станет доступен** — это даёт
возможность ЧИТАТЬ репозиторий (и, возможно, некоторые операции записи,
в зависимости от того, какие именно tools предоставляет конкретный
коннектор — не предполагай возможности заранее, проверяй фактический
список инструментов после `tool_search`). Это НЕ то же самое, что
произвольный `git clone`/`push` через bash — тот механизм технически
пробовался в этой же сессии и **сработал для публичного репозитория**
(`git clone`/`git ls-remote` через bash реально отработали для
`github.com/octocat/Hello-World`), но: (а) это не гарантированная,
документированная возможность — network allowlist может отличаться в
других сессиях, (б) для ЧАСТНОГО репозитория пользователя потребуется
аутентификация (токен), которой у ассистента нет и не должно быть
(хранение секретов между сессиями не предусмотрено), поэтому это не
практичный путь для реальной работы с приватным репо пользователя.
Единственный надёжный канал остаётся: пользователь загружает файлы в чат
→ ассистент правит → отдаёт файлы обратно. GitHub (через Project
Knowledge или коннектор, если станет доступен) — это удобство поверх
этого процесса, не замена ему.

## 11. Прокачка игрока-персонажа (scaffolding) — 22.07.2026

Пользователь прислал референс-скриншоты чужого мода/сборки (неоновый
киберпанк-стиль, сердце/часы/статы/круглый компас-виджет) и попросил
похожий кастомный HUD для СВОЕГО персонажа, не для юнита-героя.

**Важное архитектурное открытие в процессе:** в проекте УЖЕ существует
полноценная hero-система (`hero/` пакет + `unit/interfaces/HeroUnit.java`)
— уровни, опыт, мана, скилл-поинты, 4 ранжируемые способности,
воскрешение павшего героя, отрисовка портрета+уровня+опыт-бара в
`hud/PortraitRendererUnit.renderHeroLevelAndExp`. НО это всё применяется
к специальным ЮНИТАМ (типа `WraithUnit` у Monsters), не к самому игроку.
Когда пользователь чётко ответил, что хочет прокачивать именно
персонажа (не юнита) — стало ясно, что нужна параллельная, отдельная
система, не переиспользование `hero/` напрямую (хотя многие ИДЕИ оттуда
пригодились как образец — SavedData-паттерн, структура уровня/опыта).

**Ключевые решения, принятые в диалоге перед началом кодинга** (важно
помнить при дальнейшей работе над этой фичей):
1. Прокачка сохраняется НАВСЕГДА (между матчами/релогами), не сбрасывается
   как RTS-ресурсы каждый матч.
2. Три характеристики (тело/разум/кулак) дают ТОЛЬКО разблокировку
   контента (что можно строить/производить в будущем дереве), БЕЗ прямых
   боевых бонусов персонажу.
3. Источники Эйдоса: убийство любого вражеского моба/юнита (подтверждено,
   работает как ваниль-XP) + мини-игры (не определено, что это) +
   "Эйдос-ферма" (не определено, что это) — оба последних явно отложены
   пользователем на будущее обсуждение.
4. Эйдос — это просто перекрашенный ваниль-опыт (текстура+название), НЕ
   отдельная экономика/ресурс. Пользователь явно сказал не переусложнять.
5. Ваниль HP-бар и XP-бар должны быть СКРЫТЫ ВСЕГДА (не только в RTS-
   матче) — чтобы не было "по 2 счётчика" одновременно.
6. Готовых арт-ассетов для HUD (сердце/часы/рамки) у пользователя пока
   нет — пользователь явно согласился на программные заглушки как
   временное решение.

**Реализация (см. раздел 0 выше для актуального списка файлов) следует
паттерну, уже проверенному в проекте для похожих задач:**
- `SavedData` для персистентности — скопировано с `unit/
  HeroUnitSaveData.java` почти 1:1, только ключ по `ownerName` вместо
  per-unit uuid.
- Сетевой пакет — `ALL`-broadcast с client-side фильтром по имени
  игрока, тот же паттерн что `ResourcesClientboundPacket` уже использует
  для другого личного per-player состояния (ресурсов) в этом же проекте
  — не изобретался новый способ распространения пакетов.
- Команда — скопирована структурно с `rtsapi-change-resources` (тот же
  файл `commands/CommandsServerEvents.java`), permission level 2 (OP),
  топ-левел (НЕ вложена под `/reignofnether` — важно: несмотря на то что
  комментарий в коде сначала ошибочно указывал `/reignofnether formix-stat`,
  на деле команда регистрируется как отдельная `/formix-stat`, как и
  соседняя `rtsapi-change-resources`; комментарий исправлен на месте).

**Обнаруженные и исправленные при написании ошибки (до того, как они
стали бы багами компиляции/выполнения):**
1. Изначально написал `@Mod.EventBusSubscriber` на
   `FormixPlayerProgressionServerEvents` — этот проект НЕ использует
   Forge-автоматическую регистрацию событий, а централизует всё через
   `registrars/ServerEventRegistrar.java`
   (`vanillaEventBus.register(XxxServerEvents.class)` вручную для каждого
   класса). Если бы не исправил — класс тихо не подписался бы ни на одно
   событие, без единой ошибки компиляции — просто ничего бы не работало
   при логине игрока. Исправлено: убрал аннотацию, добавил явную
   регистрацию в `ServerEventRegistrar`.
2. По памяти чуть не использовал несуществующую
   `VanillaGuiOverlay.EXPERIENCE_LEVEL` константу (число уровня в ваниле
   рисуется КАК ЧАСТЬ `EXPERIENCE_BAR` overlay, отдельной константы для
   него нет). Проверено веб-поиском перед использованием, использую
   только подтверждённые `PLAYER_HEALTH` и `EXPERIENCE_BAR`.
3. Неиспользуемая переменная `statIconSize` осталась после правки плана
   (сначала хотел рисовать иконки для статов, потом решил ограничиться
   текстом для этого прототипа) — убрана при финальной проверке.

**Как это тестировать сейчас:** зайди в игру, выполни (нужны права OP)
`/formix-stat body 3 <своё_имя>`, `/formix-stat level 5 <своё_имя>`,
`/formix-stat eidos 120 <своё_имя>` и т.д. — числа должны появиться в
кастомном HUD внизу экрана, ваниль-сердца/полоска опыта должны пропасть
полностью (в любом режиме игры, не только в RTS-матче).

## 12. Ресурсы: переименование + арт, зона застройки, мгновенная добыча — 22-23.07.2026

Три отдельные фичи одной волны правок, вынесены сюда одним разделом
(были ранее только в разделе 0, откуда убраны при компрессии 26.07.2026 —
см. правило в начале раздела 0 про то, почему это произошло).

### 12.1 Ресурсы переименованы, получили кастомный арт (23.07.2026)

Пользователь прислал 4 готовых PNG (настоящий арт, не программные
заглушки) и попросил переименовать ресурсы:
- food → "Рис" (иконка заменила `textures/icons/items/wheat.png`)
- wood → "Эфир" (заменила `textures/icons/items/wood.png`)
- ore → "Версиформ" (заменила `textures/icons/items/iron_ore.png`)
- Эйдос получил свою иконку (`textures/hud/formix_eidos.png`, новый файл)

**Важная деталь:** названия ресурсов сами по себе НИГДЕ не читались из
`en_us.json` ключей `resources.reignofnether.food/wood/ore` — реальный
текст "food"/"wood"/"ore" был жёстко вписан в 41 отдельную строку
локализации (тултипы, ошибки "not enough X", чат при передаче ресурсов
союзникам, туториал, sandbox custom buildings). Все 41 строка переименованы
через Python json-парсинг (не поиском-заменой вслепую, чтобы не задеть
JSON-структуру/экранирование).

**Иконки заменены "на месте"** (те же имена файлов) — это разом обновило
все точки использования, включая кастомный bitmap-font механизм
(`font/resource_icons.json`), который переиспользует эти PNG как символы
`\uE000`-`\uE002` внутри строк тултипов (`ResourceCosts.getFormattedCost`)
— неочевидный механизм, специально проверен отдельно, подтверждено что
покрывается той же заменой файлов.

### 12.2 "Уровень 2" фич: зона застройки + мгновенная добыча + рыбалка (22.07.2026)

- ✅ **Граница застройки.** `BuildingUtils.BUILD_ZONE_RADIUS = 80`,
  метод `canPlaceWithinBuildZone()` — проверяет дистанцию до ближайшего
  СВОЕГО capitol-здания (`isCapitol=true`, общее для всех фракций).
  Встроено в `BuildingServerEvents.placeBuilding()` как жёсткое серверное
  правило (не только визуал). Первое здание игрока (когда capitol ещё
  нет) всегда разрешено. Визуальный синий круг — отдельный, независимый
  от `RangeIndicatorAddon` рендер-путь (`blocks/BlockClientEvents.
  onRenderLevel`), специально не переиспользует существующий индикатор,
  чтобы не конфликтовать с militia/warrior-range кругом у некоторых
  capitol-зданий. **Известный, подтверждённый НЕ-баг:** этот круг рисуется
  независимо от RTS/orthoview режима (в отличие от кастомного HUD
  прокачки персонажа, который в RTS-режиме скрывается) — пользователь
  подтвердил, что видеть границу в обоих контекстах — ожидаемое поведение.
- ✅ **Мгновенное зачисление ресурсов при личной добыче.** Игрок, ломающий
  блок сам (не юнитом), мгновенно получает ресурс в общий пул — добавлено
  в существующий `ResourcesServerEvents.onPlayerBlockBreak`. Не
  пересекается с тем, как юниты собирают ресурсы (`GatherResourcesGoal`
  работает через отдельный путь изменения блоков — нет риска задвоения).
- ⏳ **Рыбалка у юнитов — НЕ начата.** Требует разбора
  `unit/goals/GatherResourcesGoal.java` (553 строки, сложная существующая
  логика выбора/отслеживания целей добычи). Отложено на будущую сессию.